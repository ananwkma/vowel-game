---
phase: 17-cipher
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - cipher.html
  - index.html
autonomous: false
requirements:
  - CIPH-02
  - CIPH-04
  - CIPH-05

must_haves:
  truths:
    - "Player can tap a numbered block and all blocks with that number glow amber (selected state)"
    - "Typing a letter fills ALL blocks with that number; they turn medium charcoal (guessed state)"
    - "There is NO color feedback (correct/wrong) while playing; all guesses look identical"
    - "Assigning a letter already used by another number auto-clears the previous number (no duplicates)"
    - "When all blocks are filled and correct, the game auto-detects the win and shows the results screen"
    - "Holding the give-up button for 3s triggers a left-to-right animated reveal of the correct letters"
    - "The results screen shows 'Solved' (sage green) for a win or 'Revealed' (dusty rose) for a give-up"
  artifacts:
    - path: "cipher.html"
      provides: "Selection, typing logic, and duplicate clearing"
      contains: "assignLetter"
    - path: "cipher.html"
      provides: "3-second hold-to-give-up button and animated decode sequence"
      contains: "give-up-btn"
    - path: "cipher.html"
      provides: "Results screen overlay matching Ladder's layout"
      contains: "results-screen"
  key_links:
    - from: "hidden-input"
      to: "assignLetter()"
      via: "input event captures character and assigns to selectedNumber"
      pattern: "assignLetter"
    - from: "assignLetter()"
      to: "checkWin()"
      via: "win check triggers automatically after every fill if all blocks have a guess"
      pattern: "checkWin"
---

<objective>
Implement the interaction layer for Cipher: block selection, letter assignment with duplicate clearing, the hold-to-give-up mechanic with animated reveal, and the results screen with auto-win detection.

Purpose: Completes the gameplay loop and integrates Cipher as a fully playable game in the collection.
Output: Fully functional cipher.html and active hub link in index.html.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cipher/17-CONTEXT.md
@.planning/phases/17-cipher/17-01-SUMMARY.md
@cipher.html
@index.html
@ladder.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Selection & Letter Input</name>
  <files>cipher.html</files>
  <action>
Implement the block selection and hidden input logic.

**Logic:**
- `selectBlock(number)`: 
  - Deselect previous.
  - Set `selectedNumber`.
  - Add `.selected` class to ALL blocks with `data-number="X"`.
  - Focus `#hidden-input` to trigger mobile keyboard.
- `hidden-input` listener:
  - Capture last character.
  - Convert to Uppercase.
  - Call `assignLetter(selectedNumber, letter)`.
  - Clear input value.
  </action>
  <verify>Open cipher.html. Tap a block; confirm all matching numbers highlight in amber. Confirm the mobile keyboard opens (simulated in DevTools if needed).</verify>
  <done>Selection and keyboard input are wired correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Letter Assignment & Logic</name>
  <files>cipher.html</files>
  <action>
Implement the core game logic in `assignLetter(number, letter)`.

**Logic:**
- **Duplicate Clearing:** If `letter` is already used by another `numY` in `guesses`, delete `guesses[numY]` and update DOM for `numY` (revert to unsolved charcoal and show number).
- **Update Guesses:** Set `guesses[number] = letter`.
- **Update DOM:** Update ALL blocks with `data-number="number"`.
  - Set `textContent` to the letter.
  - Remove `.unsolved`, add `.guessed` (medium charcoal).
- **Win Check:** Call `checkWin()`.
- **Persistence:** Save `guesses` to `localStorage` (CipherProgress).
  </action>
  <verify>Type letters into the cipher. Confirm matching blocks update and duplicates are cleared. Confirm all guessed blocks look identical (no color feedback).</verify>
  <done>Letter assignment, duplicate enforcement, and guessed visual states are fully functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Give-Up Mechanic & Animated Decode</name>
  <files>cipher.html</files>
  <action>
Implement the 3-second hold-to-give-up button and the animated reveal.

**Logic:**
- Add `#give-up-btn` and `#give-up-bar` (style matching Ladder).
- Implement the `pointerdown` / `pointerup` timer logic for the 3s hold.
- On Give-Up:
  - Disable further input.
  - Trigger `animatedDecode()`: Iterate through all blocks left-to-right. For each block, replace current guess with the correct letter from `cipherMap` and apply a brief sage green highlight transition (discretionary bounce/sweep).
  - Total duration ~1.5s.
  - After animation, call `showResults({ solved: false })`.
  </action>
  <verify>Hold the give-up button. Confirm the progress bar fills. Confirm the blocks animate their reveal from left-to-right before showing results.</verify>
  <done>Give-up mechanic and animated reveal sequence are implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Win Detection & Results Screen</name>
  <files>cipher.html</files>
  <action>
Implement the results screen and win detection.

**Logic:**
- `checkWin()`: If `Object.keys(guesses).length === Object.keys(cipherMap).length`, check if all `guesses[n] === cipherMap[n]`.
- If correct: `gameState.won = true`, call `DailyStatus.markCompleted('cipher')`, and `showResults({ solved: true })`.
- `showResults(options)`:
  - Show a fixed overlay matching the Word Ladder results screen.
  - Title: "Solved" in sage green (#8BAF7C) with confetti (if win).
  - Title: "Revealed" in dusty rose (#C4836F) (if give-up).
  - Display the full decoded quote and author prominently.
  - Include "Share" and "Back to Lexicon" buttons.
  </action>
  <verify>Win the puzzle. Confirm the results overlay appears with the correct title and confetti. The decoded quote should be clearly readable.</verify>
  <done>Results screen and win detection are fully functional and consistent with other games.</done>
</task>

<task type="auto">
  <name>Task 5: Enable Cipher on Hub</name>
  <files>index.html</files>
  <action>
Update `index.html` to make the Cipher card active and integrated into the daily status check.

**Changes:**
- Remove `.disabled` class and `aria-disabled="true"` from `#card-cipher`.
- Change the `div` to an `a` tag with `href="cipher.html"`.
- Update the hub script to include `'cipher'` in the array of games being checked for daily completion.
  </action>
  <verify>Open index.html. Confirm the Cipher card is now colorful/active and navigates to the game. After winning, confirm the card shows as "Completed" in the hub.</verify>
  <done>Cipher is fully enabled and integrated into the hub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final Visual & Interaction Check</name>
  <files>cipher.html, index.html</files>
  <action>
Verify core gameplay:
1. Tap selection shows amber outline.
2. Typing fills blocks correctly.
3. 3s hold-to-give-up triggers animated reveal.
4. Win results overlay matches design.
5. Author hint is subtle.
  </action>
  <verify>Type 'approved' after testing.</verify>
  <done>Game flows and visuals verified.</done>
</task>

</tasks>

<verification>
1. All blocks filled correctly triggers win.
2. 3s hold-to-give-up triggers animated reveal.
3. No green/red feedback while playing (Requirement override from Context).
4. Hub card is active and completion status works.
</verification>

<success_criteria>
- Selection, typing, and duplicate logic work as specified.
- Give-up mechanic behaves identically to Ladder.
- Results screen uses correct colors and layouts.
- Auto-detect win on all blocks filled.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cipher/17-02-SUMMARY.md`
</output>
