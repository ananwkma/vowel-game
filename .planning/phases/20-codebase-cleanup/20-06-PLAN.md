---
phase: 20-codebase-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - ladder.html
  - vowel.html
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "index.html loads shared.js and uses DailyStatus.isCompleted() for card dimming"
    - "All console.log calls in ladder.html are inside if (IS_DEBUG) guards"
    - "vowel.html JS sections follow the standard order: CONSTANTS & CONFIG → STATE → WORD ENGINE → DAILY ENGINE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING"
  artifacts:
    - path: "index.html"
      provides: "Hub page using shared DailyStatus API"
      contains: 'shared.js'
    - path: "ladder.html"
      provides: "Word Ladder game with clean production console"
      contains: "IS_DEBUG"
    - path: "vowel.html"
      provides: "Vowel game with standard JS section ordering"
      contains: "SECTION: STATE"
  key_links:
    - from: "index.html"
      to: "shared.js"
      via: "script src tag before hub script block"
      pattern: "script src.*shared\\.js"
    - from: "index.html"
      to: "DailyStatus.isCompleted(gameId)"
      via: "Replace inline localStorage parsing"
      pattern: "DailyStatus\\.isCompleted"
    - from: "vowel.html STATE section"
      to: "position after CONSTANTS & CONFIG"
      via: "Section reorder"
      pattern: "SECTION: STATE"
---

<objective>
Close 3 verification gaps from 20-VERIFICATION.md: (1) index.html missing shared.js and using raw localStorage instead of DailyStatus, (2) ladder.html console.log calls need gating audit and confirmation, (3) vowel.html STATE section appears after WORD ENGINE and DAILY ENGINE instead of immediately after CONSTANTS & CONFIG.

Purpose: Complete the phase goal of "shared logic extracted" and "consistent JS section ordering" across all five HTML files.
Output: index.html uses DailyStatus.isCompleted(); ladder.html confirmed clean; vowel.html sections in standard order.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-codebase-cleanup/20-VERIFICATION.md
@.planning/phases/20-codebase-cleanup/20-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.html to shared.js and replace raw localStorage with DailyStatus</name>
  <files>index.html</files>
  <action>
    Read index.html in full. Make two targeted changes:

    1. Add shared.js script tag: Insert `<script src="/shared.js"></script>` on the line immediately before the existing inline `<script>` block near the bottom of `<body>` (around line 236). Do NOT add it in `<head>` — it must come before the hub script block so DailyStatus is available when the inline script runs.

    2. Replace raw localStorage parsing: The current "Mark completed games" block reads:
    ```js
    const status = JSON.parse(localStorage.getItem('wordGames_dailyStatus') || '{}');
    const today = new Date().toISOString().split('T')[0];
    ['vowel', 'ladder', 'hunt', 'cipher'].forEach(function(gameId) {
      const record = status[gameId];
      if (record && record.completed && record.dateKey === today) {
        const card = document.getElementById('card-' + gameId);
        if (card) card.classList.add('completed');
      }
    });
    ```
    Replace that block with:
    ```js
    ['vowel', 'ladder', 'hunt', 'cipher'].forEach(function(gameId) {
      if (DailyStatus.isCompleted(gameId)) {
        const card = document.getElementById('card-' + gameId);
        if (card) card.classList.add('completed');
      }
    });
    ```

    The outer `try { ... } catch(e) {}` wrapper can stay — keep it wrapping the new forEach call for safety. Remove the now-unused `status` and `today` variables.

    Do not change any other part of index.html.
  </action>
  <verify>
    Run these greps against index.html:
    - `grep -n "shared.js" index.html` — must show the script src tag
    - `grep -n "DailyStatus.isCompleted" index.html` — must show 1 match
    - `grep -n "wordGames_dailyStatus" index.html` — must return 0 matches
    - `grep -n "localStorage.getItem" index.html` — must return 0 matches
  </verify>
  <done>
    index.html includes a script src tag for /shared.js and the card-dimming logic calls DailyStatus.isCompleted(gameId) for each game. No raw localStorage.getItem('wordGames_dailyStatus') parsing remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and confirm all console.log calls in ladder.html are gated by IS_DEBUG</name>
  <files>ladder.html</files>
  <action>
    The verifier flagged 4+ ungated console.log calls in ladder.html. Before making any changes, run:
    `grep -n "console\.log" ladder.html`

    Inspect each result to determine whether it is inside an `if (IS_DEBUG)` block. A call is ungated if it appears at script scope or in a function body WITHOUT an enclosing `if (IS_DEBUG)` guard anywhere in the call chain.

    For any ungated console.log found: wrap it in `if (IS_DEBUG) { ... }`. If the call is a standalone single-line statement, convert it to `if (IS_DEBUG) console.log(...)`.

    If all console.log calls are already gated (direct code inspection suggests this may be the case), make no changes to ladder.html. Record findings in the SUMMARY regardless.

    Do not change any gameplay logic, variable names, or structure.
  </action>
  <verify>
    Run: `grep -n "console\.log" ladder.html`

    For each line in the output: read surrounding context (5 lines above) to confirm it is inside an `if (IS_DEBUG)` block. Zero ungated console.log calls should remain.
  </verify>
  <done>
    Every console.log call in ladder.html is wrapped inside an if (IS_DEBUG) guard. No production console output occurs when IS_DEBUG is false.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reorder vowel.html JS sections — move STATE to immediately after CONSTANTS & CONFIG</name>
  <files>vowel.html</files>
  <action>
    Read vowel.html. The current JS section order starting around line 996:
      CONSTANTS & CONFIG → WORD ENGINE → DAILY ENGINE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → STATE → UI RENDERING → ...

    Target standard order:
      CONSTANTS & CONFIG → STATE → WORD ENGINE → DAILY ENGINE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING → ...

    The STATE section starts with `/* ============================================================ SECTION: STATE ============================================================ */` and contains:
    - `const gameState = { ... }` object
    - Multiple `let` declarations for drag-and-drop state variables
    - `const isMobile = ...` and related mobile state variables
    - `document.getElementById('instruction-main-text').textContent = ...` line setting instruction text

    The section ends immediately before the `/* === SECTION: UI RENDERING === */` comment.

    Move strategy:
    1. Identify the exact line range of the STATE section: from the SECTION: STATE comment through to (but not including) the SECTION: UI RENDERING comment.
    2. Cut that entire block.
    3. Insert it immediately after the CONSTANTS & CONFIG section ends (after `const VOWEL_DATE_SEED = DATE_SEED;`, before the SECTION: WORD ENGINE comment).

    CRITICAL: The STATE section includes a live DOM call (`document.getElementById('instruction-main-text').textContent = ...`). This is safe because the game script runs at end of `<body>` — DOM is fully available. Moving STATE earlier in the script block does not affect DOM availability.

    Also check for forward reference issues: WORD ENGINE uses `WORDS` (from the word list, defined earlier) and `seededRandom` (from shared.js, always available). STATE uses no references to WORD ENGINE or DAILY ENGINE. The move is dependency-safe.

    Do not rename any variables, change any logic, or modify any other section.
  </action>
  <verify>
    Run: `grep -n "SECTION:" vowel.html | head -15`

    Confirm the JS sections appear in this order (CSS sections appear before line 996):
    - SECTION: CONSTANTS & CONFIG
    - SECTION: STATE  (immediately after CONSTANTS & CONFIG)
    - SECTION: WORD ENGINE
    - SECTION: DAILY ENGINE
    - SECTION: UTILITY FUNCTIONS
    - SECTION: DOM REFERENCES
    - SECTION: ENGINE

    Also run: `grep -c "SECTION:" vowel.html` — total section count must be unchanged from before the edit (section count should remain the same, just reordered).
  </verify>
  <done>
    vowel.html JS sections follow the standard order: CONSTANTS & CONFIG → STATE → WORD ENGINE → DAILY ENGINE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING. The STATE section appears immediately after CONSTANTS & CONFIG and before WORD ENGINE.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `grep -n "shared.js" index.html` — returns 1 match with script src tag
2. `grep -n "DailyStatus.isCompleted" index.html` — returns 1 match
3. `grep -n "wordGames_dailyStatus" index.html` — returns 0 matches
4. `grep -n "console\.log" ladder.html` — all results are inside IS_DEBUG guards (verify each with context)
5. `grep -n "SECTION:" vowel.html` — shows CONSTANTS & CONFIG followed immediately by STATE, then WORD ENGINE
</verification>

<success_criteria>
- index.html loads shared.js and uses DailyStatus.isCompleted() for all 4 game cards (vowel, ladder, hunt, cipher)
- ladder.html has zero ungated console.log calls (all inside IS_DEBUG guards)
- vowel.html JS section order matches standard: CONSTANTS & CONFIG → STATE → WORD ENGINE → DAILY ENGINE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING
- No gameplay regressions introduced in any of the three files
</success_criteria>

<output>
After completion, create `.planning/phases/20-codebase-cleanup/20-06-SUMMARY.md`
</output>
