---
phase: 20-codebase-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - ladder.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "ladder.html JS sections follow the standard ordering: CONSTANTS & CONFIG → STATE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING → EVENT HANDLERS → INIT"
    - "ladder.html no longer contains inline seededRandom, DailyStatus, or URL param parsing — these come from shared.js"
    - "Word Ladder game plays identically after restructure: tile interaction, path history, give-up hold, results screen all work"
    - "No console errors on ladder.html load"
  artifacts:
    - path: "ladder.html"
      provides: "Word Ladder game — fully restructured with standard section ordering"
      contains: "shared.js"
  key_links:
    - from: "ladder.html"
      to: "shared.js"
      via: "<script src='/shared.js'> before game script block"
      pattern: "src=\"/shared.js\""
    - from: "ladder.html game script"
      to: "seededRandom / DailyStatus"
      via: "calls to shared utilities (no local redefinition)"
      pattern: "seededRandom\\|DailyStatus"
---

<objective>
Perform a full readability-first restructure of ladder.html (1752 lines) following the standard section ordering established in Plan 02.

Purpose: Make Word Ladder code readable top-to-bottom with consistent sections matching the project's new standard. Remove inline utility duplicates now provided by shared.js.
Output: ladder.html fully restructured, no behavior change.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-codebase-cleanup/20-CONTEXT.md
@.planning/phases/20-codebase-cleanup/20-RESEARCH.md
@.planning/phases/20-codebase-cleanup/20-02-SUMMARY.md
@ladder.html
@shared.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit ladder.html for dead code and inconsistencies</name>
  <files>ladder.html</files>
  <action>
Before restructuring, perform a thorough audit of ladder.html (1752 lines) and make targeted removals:

**Dead code scan:**
1. Search for `console.log` calls not gated by `if (IS_DEBUG)` — remove ungated logs.
2. Search for functions defined but never called within the file — remove them.
3. Search for commented-out code blocks (multi-line `//` sections or old `/* ... */` blocks of dead logic) — remove them.
4. Search for TODO/FIXME/HACK/TEMP comments referencing removed features — remove them.
5. Check for any leftover debug artifacts (e.g., debug display divs, test word overrides not behind IS_DEBUG).

**Naming inconsistency scan:**
1. Verify the game state object is named `gameState` (consistent with project standard from RESEARCH.md).
2. Verify event handler functions use `handle` prefix (e.g., `handleTileClick` not `onTileClick` or `tileClicked`).
3. Verify boolean flags use `is/has` prefix where applicable.

**CSS section completeness scan:**
ladder.html CSS already uses the standard `SECTION:` format (confirmed in research). Verify all sections are present:
- GOOGLE FONTS, CSS VARIABLES, GLOBAL RESET, BACK TO HUB, LAYOUT, TILES, (game-specific sections), ANIMATIONS, RESPONSIVE
- If any @media queries are scattered outside SECTION: RESPONSIVE, move them to the end.
- If SECTION: RESPONSIVE is missing, add it as the final CSS section collecting all @media queries.

Make all audit-driven removals/corrections directly in the file.
  </action>
  <verify>
Run: `grep "console.log" ladder.html | grep -v "IS_DEBUG\|debug\|\[DEBUG\]\|\[Ladder\]\|\[DailyStatus\]"` — should return nothing.
Run: `grep "SECTION:" ladder.html` — should show at least 8 CSS sections and 8+ JS sections.
  </verify>
  <done>Dead code removed. State object consistently named gameState. CSS sections all present with @media queries at end. No ungated console.log calls remain.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline utilities with shared.js and enforce JS section ordering</name>
  <files>ladder.html</files>
  <action>
Complete the restructure of ladder.html following the same pattern established in vowel.html (Plan 02).

**1. Add shared.js script tag:**
Add `<script src="/shared.js"></script>` immediately before the main game `<script>` block (either in `<head>` or just before the closing `</body>` tag, consistent with how vowel.html was restructured in Plan 02 — check 20-02-SUMMARY.md for exact placement).

**2. Remove inline duplicates (now in shared.js):**
- Remove inline `seededRandom()` function — ladder.html has it (it was confirmed duplicated 4x in research).
- Remove inline `DailyStatus` object definition.
- Remove inline URL param parsing IIFE that sets DATE_SEED — replace with a game-specific constant:
  `const LADDER_DATE_SEED = DATE_SEED + '_ladder_v1';`
  Then update all references in ladder.html that used the old local DATE_SEED to use LADDER_DATE_SEED.
  Note: the `_ladder_v1` suffix was already part of the original ladder seed (per phase 15 decision log) — verify this in the code. If the existing seed constant already has `_ladder_v1`, just remove the IIFE and declare `const LADDER_DATE_SEED = DATE_SEED + '_ladder_v1';`.
- Remove inline IS_DEBUG declaration — IS_DEBUG is now from shared.js.

**3. Enforce standard JS section ordering:**
Reorder the JS script block to match the standard (from RESEARCH.md):
```
SECTION: DICTIONARY         — DICTIONARY array (large, unchanged)
SECTION: CONSTANTS & CONFIG — LADDER_DATE_SEED, OPTIMAL_PATH, OPTIMAL_STEPS, other constants
SECTION: STATE              — gameState object
SECTION: UTILITY FUNCTIONS  — isValidDictionaryWord, countLetterDiffs, isValidNextWord, buildAdjacencyMap, bfsShortestPath, local helpers
SECTION: DOM REFERENCES     — cached querySelector calls
SECTION: ENGINE             — getDailyLadderPuzzle, puzzle generation
SECTION: UI RENDERING       — renderTiles, renderHistory, addToHistory, showResults, etc.
SECTION: EVENT HANDLERS     — all addEventListener calls (tile tap, input, give-up, etc.)
SECTION: INIT               — init() + DOMContentLoaded listener
```

**4. What NOT to change:**
- DICTIONARY array content (2710-word list, unchanged)
- Any game logic (BFS, validation, tile interaction, give-up hold mechanic)
- localStorage keys
- DailyStatus.markCompleted('ladder') call
- `_ladder_v1` seed suffix
- Give-up hold mechanic (pointerdown 3s fill + pointerup cancel) — this is game-specific
- stampAnimation, shakeAnimation functions
- The results screen HTML/logic
  </action>
  <verify>
Run: `node -e "const h=require('fs').readFileSync('ladder.html','utf8'); console.log('shared.js:', h.includes('shared.js')); console.log('no inline seededRandom:', !h.includes('function seededRandom')); console.log('no inline DailyStatus:', !h.includes('const DailyStatus')); console.log('LADDER_DATE_SEED:', h.includes('LADDER_DATE_SEED'));"` — all true.
Start server and open http://localhost:3000/ladder.html — submit a valid word step, verify tile interaction works, history appends, give-up hold works.
  </verify>
  <done>ladder.html references shared.js, has no inline seededRandom/DailyStatus/URL-param IIFE. JS sections use standard "SECTION: [NAME]" ordering. Word Ladder game plays identically.</done>
</task>

</tasks>

<verification>
1. `grep "src=\"/shared.js\"" ladder.html` returns a match.
2. `grep "function seededRandom" ladder.html` returns nothing.
3. `grep "const DailyStatus" ladder.html` returns nothing.
4. Open ladder.html — complete a word step — no console errors.
</verification>

<success_criteria>
ladder.html fully restructured with standard section ordering. shared.js utilities remove ~44 lines of duplicated code. Game plays identically.
</success_criteria>

<output>
After completion, create `.planning/phases/20-codebase-cleanup/20-03-SUMMARY.md`
</output>
