---
phase: 09-mobile-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "Viewport meta tag includes viewport-fit=cover"
    - "#app uses env(safe-area-inset-top) for top padding"
    - "moveAt() consistently uses clientX/clientY for translation calculations"
  artifacts:
    - path: "index.html"
      provides: "CSS and JS fixes for mobile UX regressions"
---

<objective>
Fix the Dynamic Island title obstruction and the dragged-block vertical offset on mobile.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix Title Spacing (FIX-01)</name>
  <action>
1. Update `<meta name="viewport">` to include `viewport-fit=cover`.
2. Update `#app` CSS padding to use `safe-area-inset` values for top and bottom.
  </action>
</task>

<task type="auto">
  <name>Task 2: Fix Drag Offset (FIX-02)</name>
  <action>
1. Update `moveAt()` call in `processDragFrame` to use `ev.clientX` and `ev.clientY`.
2. Update `moveAt()` parameter names to `clientX, clientY` for clarity.
  </action>
</task>

</tasks>

<verification>
1. Verify viewport meta tag: `grep "viewport-fit=cover" index.html`
2. Verify CSS: `grep "safe-area-inset-top" index.html`
3. Verify JS: `grep "moveAt(ev.clientX, ev.clientY)" index.html`
</verification>
