---
phase: 02-block-manipulation-and-vowel-selection
plan: 06
type: execute
wave: 5
depends_on:
  - "05"
gap_closure: true
files_modified:
  - game.html
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "Vowel blocks do not snap to the top of the screen when clicked/dragged."
    - "Vowel blocks maintain their vertical position during horizontal drag."
    - "Selected vowels are correctly displayed in the vowel block after selection via the picker."
  artifacts:
    - path: "game.html"
      provides: "Fixed drag-and-drop positioning logic."
      contains:
        - "initialDraggableRect"
        - "translateY"
    - path: "game.html"
      provides: "Corrected vowel display logic."
      contains:
        - "textContent = selectedVowel"
  key_links:
    - from: "pointermove handler"
      to: "accurate vertical positioning"
      via: "JS calculations"
      pattern: "initialDraggableRect.top"
    - from: "vowelPicker.show resolve"
      to: "block text update"
      via: "JS assignment"
      pattern: "droppedElement.textContent = selectedVowel"
---

<objective>
Fix bugs reported by the user: vowel blocks snapping to the top of the screen during drag, and selected vowels not displaying in the blocks.

Purpose: Address critical functionality regressions introduced during previous implementations, ensuring the core interaction works as expected before final human verification.

Output: A bug-free `game.html` where drag-and-drop behaves correctly and vowel selection updates the block display.
</objective>

<context>
@.planning/phases/02-block-manipulation-and-vowel-selection/02-RESEARCH.md
@.planning/phases/02-block-manipulation-and-vowel-selection/02-05-SUMMARY.md
@./game.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Block Snapping and Vertical Movement During Drag</name>
  <files>game.html</files>
  <action>
    1.  **Review `pointerdown` and `pointermove`:** Carefully re-examine the `pointerdown` and `pointermove` event handlers within the drag-and-drop logic in `game.html`.
    2.  **Correct Vertical Positioning:**
        -   The `currentDraggable.style.top` and `currentDraggable.style.left` are set relative to `gameBoardRect`. However, the `initialDraggableRect` itself is relative to the viewport.
        -   Ensure that the `translateY` component of the `transform` style for `currentDraggable` is correctly calculated to maintain its original vertical position, or that `currentDraggable.style.top` is set appropriately relative to the `gameBoard`'s top.
        -   The `initialDraggableRect.top` should be stored and used as a baseline. The `translateY` value should be `0` or explicitly set to compensate for any unintended vertical shifts.
        -   Verify that `event.pageY` or `event.clientY` is not being used incorrectly to set vertical position during `pointermove`. The task specifies *only horizontal* movement.
    3.  **Ensure `initialDraggableRect` is correct:** Confirm `initialDraggableRect` captures the draggable's position *before* any `position: absolute` changes.
  </action>
  <verify>
    - Open `game.html` in a browser.
    - Drag a yellow block: it should move only horizontally and maintain its vertical alignment on the screen relative to other blocks. It should not "snap" to the top or change vertical position unexpectedly.
  </verify>
  <done>
    Vowel blocks now maintain their vertical position during horizontal drag and do not snap to the top of the screen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Vowel Display After Selection</name>
  <files>game.html</files>
  <action>
    1.  **Review `vowelPicker` usage:** Examine the code that handles the resolution of the `vowelPicker.show()` promise, specifically where `selectedVowel` is supposed to update the block's content.
    2.  **Identify the target element:** Ensure that the correct block element (`droppedElement` or the equivalent currently selected block) is being targeted for the `textContent` update.
    3.  **Correct `textContent` assignment:** Verify that `droppedElement.textContent = selectedVowel;` or similar logic is correctly implemented and executed after a vowel is selected from the picker.
    4.  **Check for conflicts:** Ensure no other part of the code is inadvertently resetting the block's `textContent` to '_' or an empty string after selection.
  </action>
  <verify>
    - Open `game.html` in a browser.
    - Right-click (or long-press) a yellow block to open the vowel picker.
    - Select a vowel (e.g., 'A').
    - The picker should disappear, and the yellow block should now display the selected vowel 'A'.
  </verify>
  <done>
    Selected vowels are now correctly displayed in the corresponding vowel block.
  </done>
</task>

</tasks>

<verification>
- Vowel blocks do not snap to the top of the screen when clicked or dragged.
- Vowel blocks maintain their vertical position during horizontal drag.
- Selected vowels are correctly displayed in the vowel block after selection via the picker.
</verification>

<success_criteria>
- The drag-and-drop interaction is visually stable and predictable.
- The vowel selection process is fully functional, updating the block display as expected.
- All user-reported bugs from the previous verification have been resolved.
</success_criteria>

<output>
Creates `.planning/phases/02-block-manipulation-and-vowel-selection/02-06-SUMMARY.md` on completion.
</output>
