---
phase: 11-backend-implementation
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: [public/index.html, server/index.js, test-api.js]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Server serves the game frontend from the public directory"
    - "API functionality is verified locally using a test script"
    - "Database persists data correctly after server restart"
  artifacts:
    - path: "public/index.html"
      provides: "The game frontend served by the backend"
    - path: "test-api.js"
      provides: "Verification script for local testing"
---

<objective>
Configure static file serving and perform local verification of the backend.

Purpose: To ensure the full stack works locally and is ready for frontend integration.
Output: Moved index.html, updated server.js, and a passing test-api.js.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-backend-implementation/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static File Serving</name>
  <files>public/index.html, server/index.js</files>
  <action>
    Create a `public/` directory and move `index.html` into it.
    Update `server/index.js` to serve static files from the `public` folder using `express.static('public')`.
    Ensure the root route `/` serves `index.html`.
  </action>
  <verify>
    Start the server and visit `http://localhost:3000` in a browser (or use curl) to confirm the game loads.
  </verify>
  <done>
    Server successfully serves the frontend game.
  </done>
</task>

<task type="auto">
  <name>Task 2: Local Verification Script</name>
  <files>test-api.js</files>
  <action>
    Create `test-api.js` that uses `fetch` (standard in Node 18+) to:
    - Submit a few scores to `POST /api/scores`.
    - Fetch stats from `GET /api/stats` and verify the percentile logic (e.g., if only one score exists, percentile should be 50).
    - Log the results clearly to the console.
    
    Ensure NO `git push` or remote calls are made in the script.
  </action>
  <verify>
    Run `node test-api.js` while the server is running and check for successful output.
  </verify>
  <done>
    API functionality is fully verified locally.
  </done>
</task>

</tasks>

<verification>
Run `node test-api.js` and verify that all tests pass. Visit `localhost:3000` to confirm the game UI is still visible.
</verification>

<success_criteria>
- `test-api.js` completes without errors.
- `localhost:3000` returns the content of `index.html`.
- No remote calls or pushes were performed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-implementation/11-03-SUMMARY.md`
</output>
