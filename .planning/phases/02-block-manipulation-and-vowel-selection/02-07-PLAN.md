---
phase: 02-block-manipulation-and-vowel-selection
plan: 07
type: execute
wave: 6
depends_on:
  - "06"
gap_closure: true
files_modified:
  - game.html
autonomous: true
requirements:
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "The vowel picker appears automatically when a vowel block is dropped."
    - "The vowel picker overlays the dropped block."
    - "The vowel picker visually resembles a slider, allowing selection of vowels by 'scrolling' (e.g., using up/down arrow keys or mouse wheel, if applicable, or just a compact vertical list)."
    - "Right-click/long-press can still activate the picker as an alternative."
  artifacts:
    - path: "game.html"
      provides: "Modified drag-and-drop handler to activate picker on drop."
      contains: "vowelPicker.show(droppedElement)"
    - path: "game.html"
      provides: "Updated CSS and JS for overlaying and 'slider-like' vowel picker."
      contains:
        - "pickerEl.style.transform = `translate"
        - "transform: translateY"
  key_links:
    - from: "pointerup event (on drop)"
      to: "vowelPicker.show() call"
      via: "JS event handler"
      pattern: "vowelPicker.show\("
    - from: "vowelPicker.show()"
      to: "pickerEl position update"
      via: "JS calculation"
      pattern: "pickerEl\.style\.top = `\$"
---

<objective>
Implement user feedback: Make the vowel picker appear automatically on block drop and redesign it to overlay the dropped block like a slider, allowing selection similar to a date picker.

Purpose: Address user feedback to refine the core interaction, enhancing usability and aligning the UI/UX with the desired visual metaphor.

Output: An updated `game.html` with improved vowel picker activation and presentation.
</objective>

<context>
@.planning/phases/02-block-manipulation-and-vowel-selection/02-RESEARCH.md
@.planning/phases/02-block-manipulation-and-vowel-selection/02-06-SUMMARY.md
@./game.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate Vowel Picker on Block Drop</name>
  <files>game.html</files>
  <action>
    1.  **Locate `pointerup` handler:** Find the `pointerup` event handler within the drag-and-drop logic in `game.html`.
    2.  **Call `vowelPicker.show()`:** After the `droppedElement` has been re-inserted into the `gameBoard` at its final position, but before `currentDraggable` is cleared, call `await vowelPicker.show(droppedElement);`.
    3.  **Handle Selection:** Upon resolution of the promise returned by `vowelPicker.show()`, update the `droppedElement.textContent` with the selected vowel.
    4.  **Preserve Right-Click/Long-Press:** Ensure the existing `contextmenu` and long-press logic for activating the picker remains intact as an alternative.
  </action>
  <verify>
    - Open `game.html` in a browser.
    - Drag and drop a yellow block.
    - The vowel picker should appear automatically next to the dropped block.
    - Select a vowel from the picker. The block's content should update.
    - Right-click a yellow block. The vowel picker should also appear.
  </verify>
  <done>
    The vowel picker now appears automatically on block drop, in addition to right-click/long-press.
  </done>
</task>

<task type="auto">
  <name>Task 2: Overlay Vowel Picker and 'Slider' Appearance</name>
  <files>game.html</files>
  <action>
    1.  **Modify `vowelPicker.show()` positioning:**
        -   In the `vowelPicker.show()` function, change how the picker is positioned. Instead of placing it adjacent, calculate its `top` and `left` to center it directly over the `targetElement` (the block that triggered it).
        -   You'll need `targetElement.getBoundingClientRect()` and `pickerEl.offsetWidth`, `pickerEl.offsetHeight` to center it.
    2.  **Adjust CSS for `.vowel-picker`:**
        -   Modify the CSS for `.vowel-picker` to ensure it overlays cleanly. This might involve `position: absolute`, higher `z-index`, and potentially some `transform` to fine-tune centering.
        -   The "slider" appearance implies a more compact, potentially focused-view UI. Since we only have 5 vowels, a simple vertical list with the focused item highlighted is sufficient. No actual "scrolling mechanism" for the vowels themselves is strictly required if they all fit. The visual "scrolling" can be achieved by just highlighting the focused item.
    3.  **Consider `tabindex` and focus:** Ensure that when the picker appears, focus is properly managed (e.g., set to the picker, and then back to the block after selection).
  </action>
  <verify>
    - Open `game.html` in a browser.
    - Drag and drop a yellow block (or right-click it).
    - The vowel picker should appear directly over the dropped block, centering itself on top of it.
    - Select a vowel. The block updates, and the picker disappears.
  </verify>
  <done>
    The vowel picker now overlays the block and has a slider-like appearance, improving the visual interaction.
  </done>
</task>

</tasks>

<verification>
- The vowel picker appears automatically when a vowel block is dropped.
- The vowel picker overlays the dropped block, centered.
- Vowel selection (via keyboard or click) updates the block.
- Right-click/long-press still activates the picker.
</verification>

<success_criteria>
- The user's feedback regarding picker activation and overlay appearance is fully implemented.
- The interactive experience is intuitive and visually aligned with the game's design.
</success_criteria>

<output>
Creates `.planning/phases/02-block-manipulation-and-vowel-selection/02-07-SUMMARY.md` on completion.
</output>
