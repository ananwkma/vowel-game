---
phase: 11-backend-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - server/db/index.js
  - server/db/schema.sql
autonomous: true
requirements: [BE-01]
must_haves:
  truths:
    - "Node.js project is initialized with required dependencies"
    - "SQLite database is initialized with the correct schema"
    - "Sensitive files and DB files are ignored by git"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and metadata"
    - path: "server/db/schema.sql"
      provides: "SQL definition for the scores table"
    - path: "server/db/index.js"
      provides: "Database connection and initialization logic"
---

<objective>
Initialize the backend environment and setup the SQLite database schema.

Purpose: To create the foundation for the server and data persistence.
Output: package.json, project structure, and initialized database.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-research-architecture/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization</name>
  <files>package.json, .gitignore, .env</files>
  <action>
    Initialize a new Node.js project using `npm init -y`. 
    Install dependencies: `express`, `better-sqlite3`, `cors`, `express-rate-limit`, `dotenv`.
    Configure `.gitignore` to ignore `node_modules`, `.env`, and all `.db` files (especially `scores.db`).
    Create a `.env` file with `PORT=3000` and `NODE_ENV=development`.
  </action>
  <verify>
    Run `npm list` to check dependencies.
    Check `.gitignore` for `node_modules` and `*.db`.
  </verify>
  <done>
    `package.json` exists with correct dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Database Setup</name>
  <files>server/db/schema.sql, server/db/index.js</files>
  <action>
    Create `server/db/schema.sql` with the `scores` table definition as specified in Phase 10 research:
    - id (INTEGER PRIMARY KEY)
    - user_id (TEXT)
    - puzzle_date (TEXT)
    - score_ms (INTEGER)
    - created_at (DATETIME)
    
    Create `server/db/index.js` to:
    - Initialize `better-sqlite3` connecting to `server/db/scores.db`.
    - Run the `schema.sql` to ensure the table exists on startup.
    - Export the database instance.
  </action>
  <verify>
    Run `node server/db/index.js` (temporary test) or check if `server/db/scores.db` is created.
  </verify>
  <done>
    Database is initialized with the `scores` table.
  </done>
</task>

</tasks>

<verification>
Check that `npm install` completed successfully and `server/db/scores.db` was created upon running the initialization script.
</verification>

<success_criteria>
- `package.json` contains `express` and `better-sqlite3`.
- `server/db/scores.db` exists.
- `better-sqlite3` can execute a query against the `scores` table.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-implementation/11-01-SUMMARY.md`
</output>
