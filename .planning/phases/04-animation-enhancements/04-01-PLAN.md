---
phase: 04-animation-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - game.html
autonomous: true
requirements:
  - VIS-07
must_haves:
  truths:
    - "The vowel picker component (A/E/I/O/U bar) appears with a smooth fade-in animation."
    - "The vowel picker component (A/E/I/O/U bar) disappears with a smooth fade-out animation."
  artifacts:
    - path: "game.html"
      provides: "CSS for vowel guide fade animations and updated JavaScript for triggering them."
      min_lines: 10
  key_links:
    - from: "game.html"
      to: ".vowel-guide CSS"
      via: "JavaScript adding/removing classes to trigger fade-in/out"
      pattern: "vowel-guide"
---

<objective>
Implement a smooth fade-in and fade-out animation for the vowel picker to enhance user feedback and polish.

Purpose: To provide a more fluid and less jarring visual experience when the vowel selection guide appears and disappears during block manipulation.
Output: The `game.html` file with updated CSS and JavaScript to manage the vowel picker's fade animations.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@game.html
</context>

<tasks>

<task type="auto">
  <name>Add CSS for Vowel Guide Fade Animation</name>
  <files>game.html</files>
  <action>
    Locate the `SECTION: VOWEL GUIDE COLUMN` in the `<style>` block of `game.html`.
    Add CSS rules for `.vowel-guide` to include `opacity: 0;` and `transition: opacity 0.2s ease-in-out;`.
    Define a new class `.vowel-guide.is-visible` that sets `opacity: 1;`.
    Ensure `pointer-events: none;` remains on the default `.vowel-guide` and is only removed temporarily if necessary for interaction (not needed for fade).
  </action>
  <verify>
    Inspect the `game.html` file to ensure the new CSS rules for `.vowel-guide` and `.vowel-guide.is-visible` are present in the `<style>` block.
  </verify>
  <done>
    The `game.html` file contains CSS to enable opacity transitions and a class to control visibility for the vowel guide.
  </done>
</task>

<task type="auto">
  <name>Integrate Fade Animation into Vowel Guide JavaScript</name>
  <files>game.html</files>
  <action>
    Locate the IIFE `(function() { ... })();` containing the drag and drop logic.
    Modify the `createGuideColumn` function:
    - After `gameBoard.appendChild(guideEl);`, add `requestAnimationFrame(() => guideEl.classList.add('is-visible'));` to trigger the fade-in.
    Modify the `removeGuideColumn` function (which is aliased from `window._removeGuideColumn`):
    - When `guideEl` is present, first remove the `is-visible` class: `guideEl.classList.remove('is-visible');`.
    - Then, use `setTimeout` with a duration slightly longer than the CSS transition (e.g., 200ms) before `guideEl.parentNode.removeChild(guideEl);` to allow the fade-out to complete. If `guideEl` does not have a parent at that point, handle gracefully.
  </action>
  <verify>
    Play the game. Drag a vowel block. The vowel guide should fade in smoothly. Release the block. The vowel guide should fade out smoothly before being removed.
  </verify>
  <done>
    The vowel picker (guide column) now fades in when created and fades out when removed, controlled by JavaScript in `game.html`.
  </done>
</task>

</tasks>

<verification>
Load `game.html` in a browser. Initiate a drag of a vowel block. Observe that the vowel guide column fades into view. Release the vowel block. Observe that the vowel guide column fades out before disappearing.
</verification>

<success_criteria>
The vowel guide (vowel picker) fades in when it appears and fades out when it disappears, as observed visually during gameplay.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animation-enhancements/04-01-SUMMARY.md`
</output>
