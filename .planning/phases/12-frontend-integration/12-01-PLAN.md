---
phase: 12-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html]
autonomous: true
requirements: [FE-01, FE-02, FE-03]

must_haves:
  truths:
    - "Persistent User UUID is generated and stored in localStorage"
    - "POST /api/scores is called with (userId, date, timeMs) upon puzzle completion"
    - "GET /api/stats is called to retrieve real player statistics"
    - "Results screen handles loading state and displays 'Better than X% of players' using real data"
    - "Graceful fallback to simulated stats occurs if the backend API is unreachable"
  artifacts:
    - path: "public/index.html"
      provides: "Backend integration logic and updated results UI"
  key_links:
    - from: "showPuzzleComplete()"
      to: "/api/scores"
      via: "fetch POST"
    - from: "showPuzzleComplete()"
      to: "/api/stats"
      via: "fetch GET"
---

<objective>
Connect the game frontend to the Node.js backend to enable persistent player tracking, score submission, and real-time statistics display.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@public/index.html
@test-api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Persistent User Identity</name>
  <files>public/index.html</files>
  <action>
    Add a `getUserId()` function to the script section that:
    1. Checks `localStorage` for a `vowel_user_id`.
    2. If missing, generates a new UUID using `crypto.randomUUID()` (or a fallback if not available).
    3. Saves and returns the ID.
    
    Update the `puzzleState` initialization to include `userId: getUserId()`.
  </action>
  <verify>
    Clear localStorage, reload the page, and check `localStorage.getItem('vowel_user_id')`. It should be present and remain consistent across reloads.
  </verify>
  <done>
    `puzzleState.userId` is populated with a persistent UUID.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Score Submission and Stats Fetching</name>
  <files>public/index.html</files>
  <action>
    Implement two new async functions in the script section:
    
    1. `apiSubmitScore(timeMs)`:
       - Sends a `POST` request to `/api/scores` with `{ userId, date, timeMs }`.
       - `date` should be `DailyEngine.dateSeed`.
       - Logs success/failure to console.
    
    2. `apiFetchStats(timeMs)`:
       - Sends a `GET` request to `/api/stats?date=${date}&timeMs=${timeMs}`.
       - Returns the JSON stats or throws an error if the request fails.
    
    Add a loading indicator state to the UI (e.g., a "Loading stats..." message) that can be shown in the results screen.
  </action>
  <verify>
    Complete a puzzle (or trigger `showPuzzleComplete()` manually in console) and observe network tab for successful API calls to /scores and /stats.
  </verify>
  <done>
    Backend communication functions are implemented and ready for UI integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Results Screen with Real Stats and Fallback</name>
  <files>public/index.html</files>
  <action>
    Modify `showPuzzleComplete()` to:
    1. Display an initial "Loading..." state in the results screen.
    2. Call `apiSubmitScore(totalSeconds * 1000)` in the background (fire and forget).
    3. Attempt to fetch real stats using `apiFetchStats(totalSeconds * 1000)`.
    4. If successful: Update the "Better than X% of players" text with `stats.percentile` and optionally display total players.
    5. If failed (backend down/error): Fall back to `PerformanceStats.getPercentile(totalSeconds)` and display the simulated rank.
    
    Ensure the "Better than X% of players" line is updated correctly in both cases.
  </action>
  <verify>
    1. Play with backend running: Verify real stats appear (percentile should match backend logic).
    2. Stop backend: Verify "Better than X% of players" still appears (using simulated stats).
  </verify>
  <done>
    The results screen displays real-time statistics with a graceful fallback to simulation.
  </done>
</task>

</tasks>

<verification>
Start the backend server (`node server/index.js`), play through the 5 daily words, and confirm the results screen displays stats. Check server logs to ensure the score was recorded.
</verification>

<success_criteria>
- User UUID persists in localStorage.
- Network requests are sent to backend upon completion.
- Results screen shows stats (real or simulated depending on backend status).
- No game-breaking errors occur if the backend returns an error or is unreachable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-integration/12-01-SUMMARY.md`
</output>
