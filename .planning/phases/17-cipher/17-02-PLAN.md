---
phase: 17-cipher
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - cipher.html
  - index.html
autonomous: false
requirements:
  - CIPH-02
  - CIPH-04
  - CIPH-05

must_haves:
  truths:
    - "Tapping a block highlights ALL occurrences of its number in amber (#D4A574)"
    - "Typing a letter fills ALL blocks with that number; they turn medium charcoal (#5A5956)"
    - "There is NO correct/wrong (green/rose) color feedback while playing; all guesses look identical"
    - "When all blocks are filled, the game auto-detects the win and shows results"
    - "Holding give-up for 3s triggers a left-to-right animated decode (~1.5s) before results"
    - "Results title: 'Solved' (sage green #8BAF7C) with confetti OR 'Revealed' (dusty rose #C4836F)"
    - "The results overlay is fixed and matches the Ladder results screen layout"
  artifacts:
    - path: "cipher.html"
      provides: "Selection, duplicate-clearing logic, and auto-win check"
      contains: "assignLetter"
    - path: "cipher.html"
      provides: "3s hold-to-give-up button and 1.5s animated reveal"
      contains: "animatedDecode"
    - path: "cipher.html"
      provides: "Results screen with Solve/Reveal titles and confetti"
      contains: "results-screen"
  key_links:
    - from: "hidden-input"
      to: "assignLetter()"
      via: "input event captures character"
      pattern: "assignLetter"
    - from: "assignLetter()"
      to: "checkWin()"
      via: "triggered only after all blocks are filled"
      pattern: "Object.keys(guesses).length"
---

<objective>
Implement the core interaction loop for Cipher: selection, letter assignment with duplicate clearing, auto-win detection, the hold-to-give-up mechanic with animated reveal, and the results screen. Integration includes enabling the hub card.

Purpose: Finalizes the game's usability and integrates it as a playable daily challenge.
Output: Fully functional cipher.html and active hub link in index.html.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cipher/17-CONTEXT.md
@.planning/phases/17-cipher/17-01-SUMMARY.md
@cipher.html
@index.html
@ladder.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block Selection & Input Capture</name>
  <files>cipher.html</files>
  <action>
Implement the selection and keyboard focus logic.

**Logic:**
- `selectBlock(number)`: 
  - Clear previous selection.
  - Set `selectedNumber`.
  - Add `.selected` class to **ALL** blocks with `data-number="X"`.
  - Focus `#hidden-input` to trigger keyboard.
- `#hidden-input` listener:
  - Capture last typed char.
  - Convert to Uppercase.
  - Call `assignLetter(selectedNumber, char)`.
  - Clear input.
  </action>
  <verify>Open cipher.html. Tap a block; confirm ALL blocks with the same number show the amber outline. Verify keyboard opens on mobile simulation.</verify>
  <done>Selection and input capture are implemented with amber highlights for all matching numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Letter Assignment & Duplicate Clearing</name>
  <files>cipher.html</files>
  <action>
Implement the assignment logic in `assignLetter(number, letter)`.

**Logic:**
- **Duplicate Clearing:** If `letter` is already in `guesses` for another `numY`, delete `guesses[numY]` and update all blocks with `data-number="numY"` to their unsolved charcoal state.
- **Update Guesses:** Set `guesses[number] = letter`.
- **Update DOM:** Update **ALL** blocks with `data-number="number"`.
  - Text: `letter`.
  - Classes: Remove `.unsolved`, add `.guessed` (**#5A5956**).
  - **CRITICAL:** Ensure NO green/rose feedback here. All guessed blocks look identical.
- **Persistence:** Save `guesses` to `localStorage` (CipherProgress).
  </action>
  <verify>Assign 'A' to number 7, then assign 'A' to number 3. Confirm number 7 reverts to unsolved charcoal. Confirm all blocks for the current assignment turn medium charcoal.</verify>
  <done>Letter assignment correctly updates the DOM across all instances and clears duplicate assignments.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-Win Detection & State Handling</name>
  <files>cipher.html</files>
  <action>
Implement auto-detection and persistence.

**Logic:**
- `checkWin()`:
  - Triggered after every `assignLetter`.
  - **Auto-Detect:** Only check correctness when `Object.keys(guesses).length === Object.keys(cipherMap).length` (all numbers have a guess).
  - If all `guesses[n] === cipherMap[n]`, set `gameState.won = true`, mark completed in `DailyStatus`, and call `showResults({ solved: true })`.
- `loadProgress()`: On page load, if today's guesses exist, restore them through `assignLetter`.
  </action>
  <verify>Fill all blocks with correct letters. Confirm the game auto-detects the win without any additional button click.</verify>
  <done>Game auto-detects wins upon completion and persists state across refreshes.</done>
</task>

<task type="auto">
  <name>Task 4: Give-Up Mechanic & Animated Decode</name>
  <files>cipher.html</files>
  <action>
Implement the 3-second hold and reveal sequence.

**Logic:**
- Add `#give-up-btn` and `#give-up-bar` (styled like Ladder).
- Implement the 3-second `pointerdown` / `pointerup` timer.
- On Give-Up:
  - Disable input.
  - Trigger `animatedDecode()`: Loop through blocks left-to-right. Update each to its correct letter from `cipherMap`. Apply a brief sage green highlight transition.
  - Duration: **~1.5s**.
  - Call `showResults({ solved: false })` after reveal.
  </action>
  <verify>Hold the give-up button. Confirm the progress bar fills. Confirm the blocks reveal correct letters in a left-to-right sweep.</verify>
  <done>Give-up mechanic and animated reveal sequence are functional.</done>
</task>

<task type="auto">
  <name>Task 5: Results Screen & Hub Activation</name>
  <files>cipher.html, index.html</files>
  <action>
Implement the results overlay and enable the game card.

**Results Screen:**
- Fixed overlay matching Ladder.
- Title: "Solved" in **sage green (#8BAF7C)** + confetti (for win).
- Title: "Revealed" in **dusty rose (#C4836F)** (for give-up).
- Full decoded quote + author.
- "Share" and "Back to Lexicon" buttons.

**Hub Integration:**
- Activate `#card-cipher`: remove `.disabled`, change `div` to `a[href="cipher.html"]`.
- Update `index.html` script to include `'cipher'` in the `dailyStatus` game list.
  </action>
  <verify>Win a game. Confirm the sage green "Solved" title and confetti appear. Verify the hub card is active and shows "Completed".</verify>
  <done>Results screen matches design specs and the game is fully integrated into the hub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final Gameplay Flow Check</name>
  <files>cipher.html, index.html</files>
  <action>
Verify the full user journey:
1. Tap a block -> amber outline on all matching numbers.
2. Type a letter -> all update to medium charcoal.
3. Overwrite a letter -> duplicate cleared from elsewhere.
4. Auto-win when full -> sage green Solved title + confetti.
5. Give-up -> animated reveal -> dusty rose Revealed title.
6. Check hub card status.
  </action>
  <verify>Type 'approved' after confirming all behaviors.</verify>
  <done>Cipher implementation verified and ready for deployment.</done>
</task>

</tasks>

<verification>
1. All blocks filled correctly triggers win auto-detection.
2. 3s hold-to-give-up triggers 1.5s animated decode.
3. Results screen colors (Sage/Rose) match 17-CONTEXT.md.
4. No green/rose colors visible until the results screen/reveal.
5. Hub card is active and completion-aware.
</verification>

<success_criteria>
- Game logic enforces duplicate clearing and auto-win detection.
- Give-up mechanic behaves identically to Word Ladder.
- Visuals (38px blocks, medium charcoal guesses, amber selection) align with context.
- Hub card fully integrated.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cipher/17-02-SUMMARY.md`
</output>
