---
phase: 11-backend-implementation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: [server/services/stats.js, server/routes/api.js, server/index.js]
autonomous: true
requirements: [BE-02, API-01, API-02]

must_haves:
  truths:
    - "API endpoints for submitting scores and fetching stats exist"
    - "Percentile calculation handles small N and ties correctly"
    - "Score submission persists data to the SQLite database"
  artifacts:
    - path: "server/services/stats.js"
      provides: "Percentile and average calculation logic"
    - path: "server/routes/api.js"
      provides: "POST /api/scores and GET /api/stats endpoints"
    - path: "server/index.js"
      provides: "Server entry point and middleware configuration"
---

<objective>
Implement the core API endpoints and statistical logic for score tracking.

Purpose: To provide the functional backend for score submission and ranking.
Output: Working Express server with stats calculation.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-research-architecture/10-RESEARCH.md
@.planning/phases/11-backend-implementation/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats Service Implementation</name>
  <files>server/services/stats.js</files>
  <action>
    Implement `getStats(date, userTimeMs)` function:
    - Query `total` count of scores for the date.
    - Query `slower` count (scores > userTimeMs).
    - Query `average` score for the date.
    - Calculate `percentile` using formula: `(slower + 0.5) / total * 100`.
    - Return an object with `totalPlayers`, `averageTime`, and `percentile`.
  </action>
  <verify>
    Create a temporary test call to `getStats` with dummy data in the DB.
  </verify>
  <done>
    Stats logic correctly calculates percentiles based on research formula.
  </done>
</task>

<task type="auto">
  <name>Task 2: API Routes and Server</name>
  <files>server/routes/api.js, server/index.js</files>
  <action>
    In `server/routes/api.js`:
    - Implement `POST /scores` to insert `{ userId, date, timeMs }`.
    - Implement `GET /stats` to fetch stats for a given date and timeMs (passed as query params).
    
    In `server/index.js`:
    - Setup Express with `cors`, `express.json()`, and `express-rate-limit`.
    - Mount the API routes at `/api`.
    - Listen on the port defined in `.env`.
  </action>
  <verify>
    Start the server with `node server/index.js` and verify it listens on port 3000.
  </verify>
  <done>
    Server is running with active endpoints at `/api/scores` and `/api/stats`.
  </done>
</task>

</tasks>

<verification>
Start the server and use `curl` or a similar tool to send a dummy score and retrieve stats.
</verification>

<success_criteria>
- `POST /api/scores` returns 200/201 success.
- `GET /api/stats` returns data in JSON format matching the stats service output.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-implementation/11-02-SUMMARY.md`
</output>
