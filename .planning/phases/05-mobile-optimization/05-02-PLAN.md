---
phase: 05-mobile-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - MOB-01-touch-suppression
  - MOB-02-single-line-layout
  - MOB-03-responsive-sizing
  - MOB-04-scroll-lock
  - MOB-05-word-length-filter

must_haves:
  truths:
    - "Player can drag blocks and select vowels on a real or simulated 375px touch device without any unwanted browser behaviors"
    - "Words never wrap to two lines on mobile"
    - "Give Up button is always visible at the bottom of the screen"
    - "No Phase 1-4 functionality has regressed"
  artifacts:
    - path: "game.html"
      provides: "Verified mobile-optimized game"
      contains: "user-scalable=no"
  key_links: []
---

<objective>
Human verification of the complete mobile optimization. Confirm all locked decisions from CONTEXT.md are visually and functionally correct on a real or simulated mobile screen.

Purpose: CSS and JS changes are hard to verify by code inspection alone — touch behaviors, layout, and sizing must be confirmed by a human looking at the actual rendered result.

Output: User confirms all mobile optimizations work correctly and approves the phase as complete.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-optimization/05-CONTEXT.md
@.planning/phases/05-mobile-optimization/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verification: confirm mobile optimizations work on simulated 375px device</name>
  <action>See how-to-verify below — human verifies Claude's automated work from Plan 05-01.</action>
  <what-built>
Complete mobile optimization of game.html:
- Global touch behavior suppression (no text selection, no iOS loupe, no tap highlight, no double-tap zoom)
- Viewport locked with user-scalable=no + touch-action: manipulation
- Page scroll locked (overflow: hidden + height: 100dvh)
- Game board single-line layout (flex-wrap: nowrap)
- Responsive block sizing (42px at ≤425px, 52px on desktop)
- Vowel picker scales proportionally with blocks
- Give Up button always visible at bottom
- Word list filtered to max 7 letters
  </what-built>
  <how-to-verify>
Test in Chrome DevTools with mobile simulation (or on a real iPhone/Android device):

**Setup:**
1. Open game.html in Chrome
2. Open DevTools → Toggle device toolbar (Ctrl+Shift+M)
3. Set device to "iPhone SE" (375×667) or custom 375px width

**Check 1 — Touch suppression (simulated):**
4. Click and hold (long-press simulate) on the game title "Vowel" — no text selection box should appear
5. Click and hold on a consonant block letter — no text selection or context menu

**Check 2 — Single-line layout:**
6. Play until a 5, 6, or 7-letter word appears (or reload until you get one like BALANCE, CHAPTER, COUNTRY)
7. Confirm ALL blocks appear on a single horizontal row — no block on a second line
8. Confirm there is no horizontal scrollbar

**Check 3 — Block sizing:**
9. Inspect a `.block` element — confirm computed width and height are 42px
10. Inspect a `.vowel-option` element — confirm it is also 42px (same as blocks)

**Check 4 — Give Up always visible:**
11. Confirm the "Give Up" button is visible at the bottom of the 375px viewport
12. It should not be hidden behind the address bar or off-screen

**Check 5 — Gameplay intact:**
13. Drag a yellow block to a position between consonants — it should move smoothly
14. Vowel picker should appear adjacent to the block
15. Select a vowel — block shows the letter
16. Complete a valid word — green flash and auto-advance should work
17. Press Give Up — reveal animation and red flash should work

**Check 6 — Desktop no regression (switch to desktop view):**
18. Remove device simulation (toggle off device toolbar)
19. Confirm blocks are 52px on desktop (inspect `.block` computed width)
20. Confirm game plays normally on desktop

**Check 7 — Word length:**
21. Play 5-10 rounds — confirm no word with more than 7 blocks ever appears
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe any issues found (e.g., "blocks still wrap on 375px", "picker not scaling") so Claude can fix them.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when the human approves the checkpoint above. All mobile optimizations are in a single file (game.html) with no external dependencies introduced.
</verification>

<success_criteria>
- Human confirms no unwanted touch behaviors on mobile
- Human confirms blocks stay on one line for all word lengths
- Human confirms Give Up button is always accessible
- Human confirms gameplay (drag, vowel select, win/lose) works correctly on mobile simulation
- Human confirms desktop is unaffected (52px blocks, full functionality)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-optimization/05-02-SUMMARY.md` following the summary template.
</output>
