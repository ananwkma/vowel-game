---
phase: 07-timer-penalty-system
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: []
autonomous: false
requirements: [TIM-01, TIM-02, TIM-03, TIM-04, TIM-05]

must_haves:
  truths:
    - "Timer is visible and counting up from 0:00 when first word appears"
    - "Give Up button shows penalty countdown starting at 60, decrementing each second"
    - "When countdown reaches 0, the number disappears"
    - "Pressing Give Up adds the countdown value to the elapsed timer"
    - "Each new word resets the Give Up countdown to 60"
    - "puzzleState.timerElapsed contains elapsed + all penalties when puzzle completes"
  artifacts:
    - path: "index.html"
      provides: "Complete timer and penalty system"
      contains: "elapsedTimer"
  key_links:
    - from: "Give Up click"
      to: "elapsed timer display jump"
      via: "penalty applied to puzzleState.timerElapsed + elapsedTimer.setBase()"
      pattern: "timerElapsed"
---

<objective>
Human verification that all five TIM requirements work correctly as an integrated system.

Purpose: Timer mechanics are interactive and time-dependent — automated checks cannot confirm the real-time UX. A human must observe the countdown, the penalty jump, and the persistence behavior.

Output: Confirmed sign-off that Phase 7 is complete and puzzleState.timerElapsed is ready for Phase 8's results screen.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-timer-penalty-system/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Complete timer and penalty system (TIM-01 through TIM-05)</name>
  <action>
What was built:
- Elapsed timer (#elapsed-timer) counting up in M:SS format in the game header (Plan 01)
- Give Up countdown integrated into button text as "Give Up (N)" decrementing from 60 to 0 (Plan 02)
- Penalty accumulation: pressing Give Up adds countdown value to elapsed timer (Plan 02)
- Countdown reset to 60 on each new word (Plan 02)
- Timer persistence across reload via puzzleState.timerElapsed in localStorage (Plan 01)
  </action>
  <how-to-verify>
Open index.html in a browser (or use ?debug to reset state) and step through:

**Test 1 — Elapsed timer starts and runs (TIM-01):**
1. Load the page.
2. Confirm a timer is visible in the header below the pip dots, starting at 0:00.
3. Wait 5 seconds — confirm it reads 0:05.

**Test 2 — Give Up countdown (TIM-02):**
1. Confirm Give Up button reads "Give Up (60)" immediately on word load.
2. Wait 5 seconds — confirm it reads "Give Up (55)" (approximately).

**Test 3 — Penalty applied (TIM-04):**
1. Note the current Give Up countdown value (e.g., "Give Up (52)") and the elapsed timer value.
2. Press Give Up.
3. Confirm the elapsed timer immediately jumps by approximately 52 seconds.
4. Example: if elapsed was 0:08 and countdown said 52, elapsed should now show 1:00.

**Test 4 — Countdown reset on new word (TIM-05):**
1. After the auto-advance, confirm the new word loads and Give Up resets to "Give Up (60)".

**Test 5 — Free Give Up at zero (TIM-03):**
1. On a new word, wait 65 seconds.
2. Confirm Give Up button reads "Give Up" (no number, countdown reached 0).
3. Press Give Up — confirm the elapsed timer does NOT jump (no penalty added).

**Test 6 — Timer continues through transitions:**
1. Win or give up a word, note the elapsed time.
2. Wait for auto-advance — confirm the timer continues running during the 2-second delay.
3. Confirm the timer does not reset when the next word loads.

**Test 7 — Persistence across reload (TIM-01 persistence):**
1. Let the timer run to about 0:30.
2. Reload the page.
3. Confirm the timer resumes from approximately 0:30 (within 1-2s tolerance).

**Test 8 — Puzzle complete:**
1. Complete all 5 words.
2. Confirm the timer stops on the "Puzzle complete!" stub screen.
3. Open DevTools → Application → localStorage → confirm `vowel_puzzle_YYYY-MM-DD` has a non-zero `timerElapsed` value.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 tests pass, or describe any failures for debugging.</resume-signal>
</task>

</tasks>

<verification>
All 5 TIM requirements observable:
- TIM-01: Elapsed timer runs from 0:00, stops at puzzle complete, persists across reload.
- TIM-02: Give Up button shows countdown from 60 decrementing each second.
- TIM-03: When countdown hits 0, number disappears — button shows only "Give Up".
- TIM-04: Pressing Give Up adds the displayed countdown value to the elapsed timer as a penalty.
- TIM-05: Give Up countdown resets to 60 on each new word.
- puzzleState.timerElapsed in localStorage contains final total (elapsed + all penalties) after puzzle completes.
</verification>

<success_criteria>
Human tester has confirmed all 8 test scenarios pass. Phase 7 is complete. puzzleState.timerElapsed is ready for Phase 8 to display on the results screen.
</success_criteria>

<output>
After completion, create `.planning/phases/07-timer-penalty-system/07-03-SUMMARY.md`
</output>
