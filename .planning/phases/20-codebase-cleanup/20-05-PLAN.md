---
phase: 20-codebase-cleanup
plan: 05
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - hunt.html
  - index.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "hunt.html JS sections follow the standard ordering: CONSTANTS & CONFIG → STATE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING → EVENT HANDLERS → INIT"
    - "hunt.html no longer contains inline seededRandom or URL param parsing — these come from shared.js"
    - "Letter Hunt game plays identically after restructure: drag selection, word locking, phase transition, hint system, give-up all work"
    - "index.html hub is cleaned up: DailyStatus.isCompleted() used for card dimming, no dead code"
    - "No console errors on hunt.html or index.html load"
  artifacts:
    - path: "hunt.html"
      provides: "Letter Hunt game — fully restructured with standard section ordering"
      contains: "shared.js"
    - path: "index.html"
      provides: "Hub page — minor cleanup, uses shared.js DailyStatus.isCompleted"
      contains: "shared.js"
  key_links:
    - from: "hunt.html"
      to: "shared.js"
      via: "<script src='/shared.js'> before game script block"
      pattern: "src=\"/shared.js\""
    - from: "index.html"
      to: "shared.js"
      via: "<script src='/shared.js'> before hub script block"
      pattern: "src=\"/shared.js\""
---

<objective>
Perform a full readability-first restructure of hunt.html (1620 lines) plus a minor cleanup of index.html (249 lines). Both files share this plan because index.html is small enough to fold in alongside hunt.html.

Purpose: hunt.html has good CSS section structure but JS needs reordering to match the standard. index.html needs shared.js reference so it can use DailyStatus.isCompleted() instead of inline localStorage parsing.
Output: hunt.html and index.html both restructured — no behavior changes.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-codebase-cleanup/20-CONTEXT.md
@.planning/phases/20-codebase-cleanup/20-RESEARCH.md
@.planning/phases/20-codebase-cleanup/20-02-SUMMARY.md
@hunt.html
@index.html
@shared.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full restructure of hunt.html</name>
  <files>hunt.html</files>
  <action>
Perform a complete readability-first restructure of hunt.html (1620 lines) following the shared.js pattern.

**Pre-restructure audit:**
1. Remove `console.log` calls not gated by `if (IS_DEBUG)`.
2. Remove commented-out code blocks (multi-line `//` sections of dead logic).
3. Remove TODO/FIXME/TEMP comments referencing removed features.
4. Verify functions have zero callers — remove them if confirmed unused.

**CSS section audit:**
hunt.html already uses standard `SECTION:` CSS format (confirmed). Verify all sections end with SECTION: ANIMATIONS and SECTION: RESPONSIVE collecting @media queries at the end. If any @media queries are inline within component sections, move them to SECTION: RESPONSIVE at the bottom.

hunt.html's current CSS SECTION list (from research): GOOGLE FONTS, CSS VARIABLES, GLOBAL RESET, BACK TO HUB, LAYOUT, TITLE, PROGRESS INDICATOR, GRID, CELL STATES, CANVAS OVERLAY, CATEGORY DISPLAY, HINT BUTTON, GIVE-UP BUTTON, BUTTONS, RESULTS SCREEN, HELP MODAL, ANIMATIONS.

Ensure SECTION: RESPONSIVE exists as the final CSS section collecting all @media queries.

**1. Add shared.js script tag:**
Add `<script src="/shared.js"></script>` immediately before the main game `<script>` block (same placement as other restructured files).

**2. Remove inline duplicates (now in shared.js):**
hunt.html has these sections to remove:
- `SECTION: SEEDED PRNG` — remove the inline `seededRandom()` function.
- `SECTION: DEBUG MODE` — remove inline IS_DEBUG declaration.
- `SECTION: DAILY SEED` — this is the DATE_SEED IIFE. Remove it and replace with:
  `const HUNT_DATE_SEED = DATE_SEED + '_hunt_v1';`
  Update all references to the old DATE_SEED in hunt.html to use HUNT_DATE_SEED.
- `SECTION: DAILY STATUS` — remove the inline DailyStatus object.

**3. Enforce standard JS section ordering:**
hunt.html's current JS sections: CONSTANTS & WORD DATA → SEEDED PRNG → DEBUG MODE → DAILY SEED → GRID GENERATION → GAME STATE → PROGRESS (pips) → DAILY STATUS → RESULT PERSISTENCE → DOM RENDER → RESULTS SCREEN → DRAG SELECTION → CANVAS → SELECTION EVALUATION → HINT SYSTEM → PHASE MANAGEMENT → GIVE-UP → HELP MODAL → CONFETTI → SHARE → DOMCONTENTLOADED boot.

After removing shared.js candidates, reorder to standard:
```
SECTION: CONSTANTS & WORD DATA — word lists, category data, grid constants (unchanged content)
SECTION: CONSTANTS & CONFIG    — HUNT_DATE_SEED, game config constants
SECTION: STATE                 — gameState object (was SECTION: GAME STATE)
SECTION: UTILITY FUNCTIONS     — local helpers not in shared.js
SECTION: DOM REFERENCES        — cached querySelector calls (if a dedicated block exists)
SECTION: ENGINE                — GRID GENERATION logic + PROGRESS (pips) helpers
SECTION: RESULT PERSISTENCE    — localStorage save/load for hunt state
SECTION: UI RENDERING          — DOM RENDER + RESULTS SCREEN + SHARE
SECTION: DRAG SELECTION        — drag/pointer event handlers
SECTION: CANVAS                — canvas drawing helpers
SECTION: SELECTION EVALUATION  — word evaluation, phase management
SECTION: HINT SYSTEM           — hint logic
SECTION: PHASE MANAGEMENT      — phase transition logic (revealCategory, etc.)
SECTION: GIVE-UP               — give-up confirm logic
SECTION: CONFETTI              — triggerConfetti (kept inline — game-specific)
SECTION: HELP MODAL            — help toggle
SECTION: EVENT HANDLERS        — remaining addEventListener calls not in above sections
SECTION: INIT                  — DOMContentLoaded boot (was DOMCONTENTLOADED boot)
```

This is a logical reordering — move the removed sections out and keep the rest in dependency order. Constants/State/Utils before Engine before UI before Events before Init.

**4. What NOT to change:**
- WORD_DATA array content (categories + words)
- Grid generation algorithm (backtracking letter placement)
- Drag selection (Pointer Events + canvas trace)
- Phase state machine (easy → revealCategory → hard → endGame)
- isAdjacentToLast guard
- Hint system and penalty logic
- Two-tap give-up confirm
- DailyStatus.markCompleted('hunt') call
- confetti function (keep inline — hunt has its own confetti closure with local variable)
- `_hunt_v1` seed namespace suffix
- Result persistence (localStorage save/restore)
  </action>
  <verify>
Run: `node -e "const h=require('fs').readFileSync('hunt.html','utf8'); console.log('shared.js:', h.includes('shared.js')); console.log('no inline seededRandom:', !h.includes('function seededRandom')); console.log('no inline DailyStatus:', !h.includes('const DailyStatus')); console.log('HUNT_DATE_SEED:', h.includes('HUNT_DATE_SEED'));"` — all true.
Start server and open http://localhost:3000/hunt.html — drag-select a word in the grid, verify it locks amber/green. Verify category reveal works. No console errors.
  </verify>
  <done>hunt.html references shared.js. No inline seededRandom, DailyStatus, or URL param IIFE. JS sections follow standard order. Letter Hunt game plays identically.</done>
</task>

<task type="auto">
  <name>Task 2: Minor cleanup of index.html</name>
  <files>index.html</files>
  <action>
index.html is 249 lines and already well-organized per research. Apply targeted cleanup only — do not over-restructure.

**1. Add shared.js script tag:**
Add `<script src="/shared.js"></script>` immediately before the hub's `<script>` block (or in `<head>` before the style block — whichever is consistent with other files).

**2. Update DailyStatus usage:**
index.html currently reads `localStorage.getItem('wordGames_dailyStatus')` inline to check which games are completed for card dimming. Replace this inline localStorage parsing with `DailyStatus.isCompleted(gameId)` calls from shared.js. This makes the hub's completion check consistent with how games mark completion.

Specifically, find the code block in index.html that reads the dailyStatus key and dims completed cards. Replace the raw `JSON.parse(localStorage.getItem(...))` approach with:
```javascript
// Use DailyStatus from shared.js for consistent completion checking
const completedGames = ['vowel', 'ladder', 'cipher', 'hunt'].filter(id => DailyStatus.isCompleted(id));
completedGames.forEach(id => {
  const card = document.querySelector(`[data-game="${id}"]`);
  if (card) card.classList.add('completed');
});
```
(Adjust attribute selectors to match what index.html actually uses for game cards — check the actual HTML attributes in index.html before writing.)

**3. Dead code scan:**
- Remove any `console.log` not gated by IS_DEBUG.
- Remove commented-out code blocks.
- Remove any placeholder content or TODO comments.

**4. What NOT to change:**
- Card layout (3 game cards + any inactive slots)
- Navigation links to vowel.html, ladder.html, cipher.html, hunt.html
- Any existing animations or transitions (fadeIn on body, etc.)
- The card HTML structure itself
  </action>
  <verify>
Run: `node -e "const h=require('fs').readFileSync('index.html','utf8'); console.log('shared.js:', h.includes('shared.js')); console.log('no raw localStorage DailyStatus parse:', !h.includes('wordGames_dailyStatus'));"` — both true.
Open http://localhost:3000/index.html — all game cards visible. Complete a game (e.g., vowel), return to hub — the completed card should be dimmed (DailyStatus.isCompleted check works).
  </verify>
  <done>index.html references shared.js. DailyStatus completion checking uses DailyStatus.isCompleted() instead of raw localStorage parsing. Hub displays correctly.</done>
</task>

</tasks>

<verification>
1. `grep "src=\"/shared.js\"" hunt.html index.html` returns matches for both files.
2. `grep "function seededRandom" hunt.html` returns nothing.
3. `grep "const DailyStatus" hunt.html` returns nothing.
4. Open hunt.html — drag-select 3 letters — no console errors.
5. Open index.html — hub loads — all game cards visible — no console errors.
</verification>

<success_criteria>
hunt.html fully restructured with standard section ordering and shared.js utilities. index.html cleaned up to use DailyStatus.isCompleted() from shared.js. Both games/pages function identically to before.
</success_criteria>

<output>
After completion, create `.planning/phases/20-codebase-cleanup/20-05-SUMMARY.md`
</output>
