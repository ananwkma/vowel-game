---
phase: 17-cipher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cipher.html
  - index.html
autonomous: true
requirements:
  - CIPH-01
  - CIPH-03

must_haves:
  truths:
    - "Opening cipher.html shows a page with a daily quote rendered as numbered blocks grouped by word"
    - "Each cipher block displays its number and is styled in deep charcoal (unsolved state)"
    - "The author attribution is visible below the blocks as a subtle hint (low opacity) from the start"
    - "The quote is the same for all players on the same calendar day (daily seed)"
    - "The hub (index.html) shows a disabled Cipher card as the 4th card in the grid"
  artifacts:
    - path: "cipher.html"
      provides: "Game page shell with CSS, HTML structure, daily engine, and quote corpus"
      min_lines: 150
    - path: "cipher.html"
      provides: "Quote rendering as word-groups of 38x38px cipher blocks"
      contains: "word-group"
    - path: "index.html"
      provides: "Disabled Cipher card in the hub grid"
      contains: "card-cipher"
  key_links:
    - from: "cipher.html DATE_SEED"
      to: "seededRandom()"
      via: "DATE_SEED includes _cipher_v1 suffix"
      pattern: "_cipher_v1"
    - from: "cipher.html buildQuoteDOM()"
      to: "#quote-display"
      via: "creates .word-group divs with .cipher-block children"
      pattern: "word-group"
---

<objective>
Build the foundational shell for Cipher: the HTML/CSS skeleton, the quote corpus with a daily-seeded engine, and the DOM rendering logic that displays quotes as numbered 38x38px blocks. Also add the disabled Cipher card to the hub.

Purpose: Establishes the visual and data foundation for the substitution cipher game.
Output: cipher.html (renderable skeleton), index.html (with 4th disabled card).
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cipher/17-CONTEXT.md
@ladder.html
@index.html
@styles/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cipher.html UI Skeleton & CSS</name>
  <files>cipher.html</files>
  <action>
Create `cipher.html` with the basic UI structure and all necessary CSS following the design tokens.

**HTML Structure:**
- Mobile meta tags (apple-mobile-web-app-capable, etc.)
- Link to `styles/design-tokens.css`
- Title: "Cipher — Word Games"
- Layout: `#back-link` (LEXICON), `#app` container, `#game-title` (Cipher), `#quote-display` (flex container), `#quote-author` (subtle label), `#status-message`, and `#hidden-input`.

**CSS Requirements:**
- Import 'Playfair Display' font.
- Use `--color-bg`, `--color-primary-text`, etc. from tokens.
- `#back-link`: Fixed top-left, serif, 0.8rem, uppercase, opacity 0.7.
- `#quote-display`: Flex-wrap wrap, gap 6px, justify-content center.
- `.word-group`: Inline-flex, nowrap, gap 4px (prevents words breaking mid-line).
- `.cipher-block`: 38x38px, border-radius 5px, font-serif 700, size 0.9rem, display flex, center, cursor pointer.
- **States:**
  - `.cipher-block.unsolved`: background `var(--color-secondary)` (#2C2B28), color `var(--color-secondary-text)`.
  - `.cipher-block.guessed`: background `#5A5956` (lighter charcoal), color `var(--color-secondary-text)`.
  - `.cipher-block.selected`: outline `3px solid var(--color-primary)` (#D4A574), outline-offset 2px.
- `#quote-author`: Font size 0.75rem, opacity 0.5, margin-top 24px, text-align center, font-style italic (visible by default).
- `#hidden-input`: Opacity 0, position absolute, pointer-events none (for keyboard trigger).
  </action>
  <verify>Open cipher.html. Confirm the basic layout (title, back-link) is visible. CSS matches design tokens (warm off-white background, charcoal blocks).</verify>
  <done>cipher.html exists with complete CSS and HTML shell. Block styling follows 38x38px and charcoal color requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Daily Engine & Quote Corpus</name>
  <files>cipher.html</files>
  <action>
Add the JavaScript engine to `cipher.html` to pick a daily quote and generate the cipher mapping.

**1. Quote Corpus:**
- Create a `QUOTES` array with at least 15-20 famous quotes (Lincoln, Twain, Einstein, etc.).
- Each entry: `{ text: "...", author: "..." }`.

**2. Seeded PRNG:**
- Copy the `seededRandom` function from `ladder.html` exactly.
- Use `DATE_SEED` based on `new Date().toISOString().split('T')[0] + '_cipher_v1'`.

**3. Mapping Logic:**
- Select a quote using the RNG.
- Identify all unique letters (A-Z) in the quote.
- Assign a unique number (1 to N) to each unique letter.
- Use Fisher-Yates shuffle (with the seeded RNG) to randomize the number assignments.
- Store results in `cipherMap` (number -> actual letter) and `letterToNum` (actual letter -> number).

**4. Player State:**
- `const guesses = {};` (number -> player's assigned letter).
- `const gameState = { won: false, revealed: false };`.
  </action>
  <verify>In DevTools console, verify `cipherMap` is generated and contains a valid mapping for today's quote letters to numbers.</verify>
  <done>Daily-seeded engine selects a quote and generates a consistent number-to-letter mapping for all players on the same day.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Quote Rendering Logic</name>
  <files>cipher.html</files>
  <action>
Implement the `buildQuoteDOM()` function to render the encoded quote.

**Logic:**
- Split `QUOTE_TEXT` into words by spaces.
- For each word, create a `.word-group` div.
- Iterate through characters in the word:
  - If A-Z: Create a `.cipher-block` with `data-number="X"`. Display the number `X`.
  - If punctuation (comma, period, apostrophe): Create a `span.punct-mark` (opacity 0.7, padding-left 1px).
- Handle the author label: Set `textContent` of `#quote-author` to `— ${QUOTE_AUTHOR}`.
- Add basic click listeners to `.cipher-block` that call a stub `selectBlock(num)`.
  </action>
  <verify>Open cipher.html. Confirm the daily quote is rendered as numbered charcoal blocks, wrapped by word groups. Punctuation is visible in plaintext. Author is visible but subtle below the blocks.</verify>
  <done>Quote is rendered correctly as word-wrapped numbered blocks. Punctuation and author attribution are plaintext.</done>
</task>

<task type="auto">
  <name>Task 4: Add Disabled Cipher Card to Hub</name>
  <files>index.html</files>
  <action>
Update `index.html` to include a 4th game card for Cipher in a disabled state.

**HTML:**
- Locate the `#hub-grid`.
- Insert a new `div.game-card.disabled` with `id="card-cipher"`.
- Include an SVG preview showing small numbered blocks (charcoal and one amber).
- Use `aria-disabled="true"`.

**Note:** Ensure the hub grid layout (already 1fr 1fr) correctly handles the 4th card in a 2x2 grid.
  </action>
  <verify>Open index.html. A 4th card "Cipher" should be visible, greyed out, and non-clickable.</verify>
  <done>Cipher card appears in hub as a 4th disabled card, maintaining grid alignment.</done>
</task>

</tasks>

<verification>
1. cipher.html shows a daily quote as numbered charcoal blocks (38x38px).
2. Author attribution is visible below blocks at low opacity.
3. Refreshing the page on the same day shows the same quote and mapping.
4. Word groups wrap naturally at word boundaries.
5. index.html shows 4 game cards; Cipher is disabled.
</verification>

<success_criteria>
- daily quote is rendered as numbered blocks with punctuation and author preserved.
- Blocks are compact (38x38px) and charcoal (#2C2B28).
- Layout is responsive and word-groups prevent mid-word wrapping.
- Hub includes Cipher card placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cipher/17-01-SUMMARY.md`
</output>
