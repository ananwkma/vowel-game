---
phase: 03-game-states-and-win-conditions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [game.html]
autonomous: true
requirements: [CORE-07, CORE-08, CORE-09, WORD-04, VIS-02, VIS-03]

must_haves:
  truths:
    - "Valid word triggers green background flash and 2s auto-advance"
    - "Give Up reveals the target word correctly and triggers red background flash"
    - "Input is locked during the 2-second feedback window"
    - "Game advances to a new random word after the delay"
  artifacts:
    - path: "game.html"
      provides: "Complete game loop and state management"
  key_links:
    - from: "checkWinCondition()"
      to: "applyStateVisuals('won')"
      via: "function call on valid word"
    - from: "Give Up button"
      to: "target word reveal"
      via: "click listener"
---

<objective>
Complete the game loop by implementing robust word validation, win/lose states, a corrected "Give Up" reveal, and automatic progression.

Purpose: To provide closure to the gameplay experience and ensure players can progress through multiple puzzles seamlessly.
Output: Updated game.html with full game logic.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-states-and-win-conditions/03-RESEARCH.md
@game.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardening Validation and State Machine</name>
  <files>game.html</files>
  <action>
    Refine the existing `checkWinCondition` and `gameState` logic:
    - Ensure `WordSet` (built from `WORDS`) is used for O(1) validation.
    - Verify `checkWinCondition` triggers on `pointerup` and only when all vowel blocks have a non-empty vowel selected.
    - Implement a 'playing' state check in all interaction handlers (drag initiation, Give Up button) to prevent input during the 2-second feedback window.
    - Ensure `applyStateVisuals` handles 'won', 'gaveup', and 'playing' states with correct background colors (Sage Green #8BAF7C for win, Dusty Rose #C4836F for give-up).
  </action>
  <verify>
    Construct a valid word and observe if it triggers the win state immediately.
    Attempt to drag a block or click Give Up during the 2s win/lose delay; it should be ignored.
  </verify>
  <done>
    Game logic prevents multi-triggers and correctly manages transitions between game phases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct "Give Up" Reveal Logic</name>
  <files>game.html</files>
  <action>
    Fix the "Give Up" reveal to show the target word in its correct form:
    - When Give Up is clicked, instead of just filling existing blocks, clear the board and re-render the target word (`gameState.currentWord`) completely.
    - Vowels should be rendered as amber blocks (`vowel-block`) with their correct letters.
    - Consonants should be rendered as charcoal blocks (`consonant-block`).
    - This ensures the player sees exactly how the word was supposed to look.
    - Add a slight CSS transition (e.g., scale-in) to the revealed blocks for visual polish.
  </action>
  <verify>
    Start a puzzle, move blocks around randomly, then click "Give Up".
    The board should transform into the correctly spelled target word with appropriate block styling.
  </verify>
  <done>
    "Give Up" provides a clear, educational reveal of the target word.
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish Transitions and Auto-Advance</name>
  <files>game.html</files>
  <action>
    Polish the feedback loop and automatic progression:
    - Ensure `scheduleAutoAdvance` uses a singleton timer (`advanceTimer`) to prevent race conditions from multiple calls.
    - Adjust background-color transition speed: 0.3s for win/lose "flashes", 0.5s for returning to the neutral play state.
    - Verify that `initGame` resets all necessary state (clearing the board, resetting background, picking a new word).
    - Double-check WCAG contrast on the Give Up button in its neutral state.
  </action>
  <verify>
    Play through 3-4 rounds (some wins, some give-ups).
    Verify smooth transitions and no "double-advancing" or skipped words.
  </verify>
  <done>
    The game loop is smooth, bug-free, and visually satisfying.
  </done>
</task>

</tasks>

<verification>
- Load game.html in browser.
- Solve a word: Background turns sage green, 2s delay, next word loads.
- Give up: Background turns dusty rose, target word is revealed in correct order, 2s delay, next word loads.
- Verify no interaction possible during delays.
</verification>

<success_criteria>
- CORE-07, 08, 09 implemented and verified.
- WORD-04 (dictionary check) functional.
- VIS-02, 03 (state colors) correct.
- Game advances indefinitely with no repeats in a 20-word window.
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-states-and-win-conditions/03-01-SUMMARY.md`
</output>
