---
phase: 17-cipher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cipher.html
  - index.html
autonomous: true
requirements:
  - CIPH-01
  - CIPH-03

must_haves:
  truths:
    - "Opening cipher.html shows a daily quote rendered as numbered blocks grouped by word"
    - "Each cipher block is 38x38px and styled in deep charcoal (#2C2B28) when unsolved"
    - "All guessed blocks appear in identical medium charcoal (#5A5956) with NO green/rose feedback while playing"
    - "The author attribution is visible below the blocks at 0.5 opacity from the start"
    - "The hub (index.html) shows a disabled Cipher card as the 4th card in the grid"
  artifacts:
    - path: "cipher.html"
      provides: "Game page shell, 38px block CSS, and daily quote engine"
      min_lines: 150
    - path: "cipher.html"
      provides: "Word-grouped quote rendering logic"
      contains: "word-group"
    - path: "index.html"
      provides: "Disabled Cipher card placeholder"
      contains: "card-cipher"
  key_links:
    - from: "cipher.html DATE_SEED"
      to: "seededRandom()"
      via: "DATE_SEED includes _cipher_v1 suffix"
      pattern: "_cipher_v1"
    - from: "cipher.html CSS"
      to: ".cipher-block"
      via: "fixed 38px sizing"
      pattern: "38px"
---

<objective>
Build the visual and logic foundation for Cipher: the HTML/CSS skeleton with 38x38px compact blocks, the daily-seeded engine with quote corpus, and the initial rendering logic. Integration includes the disabled hub card.

Purpose: Establishes the core data structure and rendering pipeline for the substitution cipher.
Output: cipher.html (foundation), index.html (placeholder card).
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cipher/17-CONTEXT.md
@ladder.html
@index.html
@styles/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML Skeleton & CSS Layout</name>
  <files>cipher.html</files>
  <action>
Create `cipher.html` with the structural shell and core layout CSS.

**HTML Skeleton:**
- Standard mobile meta tags and link to `styles/design-tokens.css`.
- `#back-link` (LEXICON), `#app` container, `#game-title` (Cipher).
- `#quote-display` (flex container), `#quote-author` (subtle label).
- `#hidden-input` for keyboard capture (opacity 0).

**CSS Layout:**
- `#quote-display`: flex-wrap wrap, gap 6px, justify-content center.
- `.word-group`: inline-flex, nowrap, gap 4px (prevents breaking mid-word).
- `#quote-author`: margin-top 24px, text-align center, font-style italic, opacity 0.5, font-size 0.75rem.
- Design: Match the warm off-white background and serif typography of the Lexicon collection.
  </action>
  <verify>Open cipher.html. Confirm the title and back-link are visible. Background matches design tokens.</verify>
  <done>Basic page structure and layout CSS are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Cipher Block Styling (Compact 38px)</name>
  <files>cipher.html</files>
  <action>
Implement the compact block styling and state classes.

**CSS Requirements:**
- `.cipher-block`: **38x38px**, border-radius 5px, font-serif 700, size 0.9rem, display flex, center.
- **States (Locked Decisions):**
  - `.unsolved`: background `#2C2B28` (charcoal), color `#F8F7F4` (off-white).
  - `.guessed`: background `#5A5956` (medium charcoal), color `#F8F7F4`. **CRITICAL: No green/rose colors during play.**
  - `.selected`: outline `3px solid #D4A574` (amber), outline-offset 2px.
- Transitions: background-color and color (var(--transition-fast)).
  </action>
  <verify>Add a test block to cipher.html. Verify it is exactly 38x38px. Verify the charcoal and medium-charcoal colors match 17-CONTEXT.md.</verify>
  <done>Cipher blocks are styled according to compact 38px requirement and locked color decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Daily Engine & Mapping Logic</name>
  <files>cipher.html</files>
  <action>
Implement the seeded PRNG and daily quote selection.

**Logic:**
- Copy `seededRandom` from `ladder.html`.
- `DATE_SEED`: Use current date + `_cipher_v1`.
- `QUOTES`: Array of 20+ famous quotes with `{ text, author }`.
- Selection: Pick quote using `rng()`.
- **Mapping:**
  - Map unique letters A-Z to numbers 1..N.
  - Shuffle the number assignments using the seeded RNG.
  - Create `letterToNum` and `cipherMap` (num to letter).
  </action>
  <verify>In console, check `cipherMap`. Refresh page; verify the mapping remains identical for the same day.</verify>
  <done>Daily engine consistently selects a quote and generates a deterministic cipher mapping.</done>
</task>

<task type="auto">
  <name>Task 4: Rendering Engine & Author Hint</name>
  <files>cipher.html</files>
  <action>
Implement `buildQuoteDOM()` to render the encoded blocks.

**Logic:**
- Split quote into words.
- For each word, create a `.word-group`.
- For each letter, create a `.cipher-block` with `data-number`. Display the number initially.
- Punctuation: Render as `span.punct-mark` (opacity 0.7) preserved in plaintext.
- **Author Attribution:** Set `#quote-author` text. Ensure it is visible from the start at 0.5 opacity as a "subtle hint".
  </action>
  <verify>Confirm the quote is rendered as word-wrapped numbered blocks. Punctuation is readable. Author is visible but subtle.</verify>
  <done>Rendering logic displays the daily cipher and the subtle author hint.</done>
</task>

<task type="auto">
  <name>Task 5: Hub Integration (Disabled Card)</name>
  <files>index.html</files>
  <action>
Add the Cipher card placeholder to the hub.

**HTML:**
- Insert `div.game-card.disabled#card-cipher` in `#hub-grid`.
- SVG: Numbered block icons (charcoal/amber).
- Label: "Cipher".
- Style: Ensure the grid accommodates the 4th card (2x2 layout).
  </action>
  <verify>Check index.html. A 4th disabled card should appear in the grid.</verify>
  <done>Hub contains the Cipher card in a disabled state.</done>
</task>

</tasks>

<verification>
1. Blocks are 38x38px and deep charcoal.
2. Author is visible at 0.5 opacity below blocks.
3. Quote and mapping are daily-seeded.
4. Hub shows 4 cards.
</verification>

<success_criteria>
- cipher.html renders a word-wrapped numbered cipher.
- Blocks follow 38px sizing and charcoal color tokens.
- Author attribution is a subtle, persistent hint.
- index.html includes the new card slot.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cipher/17-01-SUMMARY.md`
</output>
