---
phase: 16-ladder-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ladder.html]
autonomous: true
requirements: [LADR-POLISH-01, LADR-POLISH-02, LADR-POLISH-03, LADR-POLISH-04]

must_haves:
  truths:
    - "No daily puzzle has fewer than 4 or more than 6 optimal steps (full-dictionary BFS)"
    - "Every daily puzzle has a common-word-only solution path of 10 steps or fewer"
    - "The absolute fallback puzzle (SCARE->STILL) satisfies both the 4-6 step range and the <=10 step common-path cap"
    - "PUZZLE_WORDS contains approximately 667 words, up from 437, increasing the pool of valid puzzle pairs"
    - "The broken STONE->CRANE fallback no longer appears anywhere in ladder.html"
  artifacts:
    - path: "ladder.html"
      provides: "Expanded PUZZLE_WORDS Set and corrected getDailyLadderPuzzle() function"
      contains: "SCARE.*STILL"
  key_links:
    - from: "getDailyLadderPuzzle()"
      to: "path.length check"
      via: "inline condition"
      pattern: "path\\.length < 5 \\|\\| path\\.length > 7"
    - from: "getDailyLadderPuzzle()"
      to: "commonPath.length check"
      via: "inline condition"
      pattern: "commonPath\\.length > 11"
    - from: "getDailyLadderPuzzle()"
      to: "fallback return"
      via: "end of function"
      pattern: "start: 'SCARE', target: 'STILL'"
---

<objective>
Polish the Word Ladder puzzle engine: expand the PUZZLE_WORDS pool from ~437 to ~667 verified common words, tighten the optimal step range from 3-7 to 4-6, add a <=10 step cap on the common-word path, and replace the broken STONE->CRANE fallback with the verified SCARE->STILL pair.

Purpose: Puzzles are currently too easy (3-step), too hard (7-step), or risk common-word paths that are absurdly long. The fallback pair silently violates the common-word guarantee. This phase fixes all four issues in a single file with surgical edits.
Output: Modified ladder.html with corrected puzzle selection logic and a larger word pool.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ladder-polish/16-CONTEXT.md
@.planning/phases/16-ladder-polish/16-RESEARCH.md
@ladder.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand PUZZLE_WORDS with ~230 verified common words</name>
  <files>ladder.html</files>
  <action>
In ladder.html, locate the `const PUZZLE_WORDS = new Set([` block (around line 729). This block ends at approximately line 812 with `]);`.

Add the following words into the Set, inserted alphabetically within their existing letter-group comments. Every word in this list has been verified against data/words5.js by the research phase — do not add any other words. Insert them so the Set remains sorted alphabetically by first letter, with existing comment markers (`// A`, `// B`, etc.) preserved.

Words to add (all verified in data/words5.js, none currently in PUZZLE_WORDS):

```
// A additions: ABOUT, ACTOR, ADULT, AFTER, AGREE, AISLE, ALBUM, ALIVE, ALLEY, APPLY, ARGUE, ASKED, ASSET, AWFUL
// B additions: BADLY, BLOWN, BLUES, BOOKS, BOOST, BORED, BONUS, BOXER, BRIDE, BRIEF, BROKE, BUYER
// C additions: CABIN, CABLE, CARRY, CHAIN, CHAIR, CHEER, CHIPS, CHUNK, CIVIL, CLIFF, CLONE, COLOR, COACH, COUNT, CREAM, CROPS, CRUEL
// D additions: DAILY, DEALT, DEBUT, DECAY, DEPOT, DEPTH, DIRTY, DISCO, DITCH, DIZZY, DOUGH, DRAIN, DRAMA, DRAPE, DRUMS, DYING
// E additions: EERIE, ELBOW, ENEMY, ENJOY, ENTER, ENVOY, ERROR, ESSAY, EVERY, ERODE
// F additions: FABLE, FALLS, FALSE, FATAL, FAULT, FAVOR, FILES, FILMS, FLASH, FLICK, FLAIR, FLING, FLINT, FLOAT, FLUNG, FOCUS, FORTY, FRAUD, FRAIL, FROTH, FROZE, FULLY, FUZZY
// G additions: GAMES, GHOST, GIFTS, GIVEN, GLARE, GLIDE, GLINT, GLOBE, GLOSS, GLOAT, GNASH, GRAVY, GRILL, GRIPS, GROUT, GROWN, GRUEL, GUILD, GUSTO
// H additions: HABIT, HANDY, HAVEN, HAZEL, HEDGE, HERBS, HINGE, HIPPO, HITCH, HOLES, HOLLY, HOMES, HOOKS, HUMOR, HUSKY, HUTCH, HURRY
// I additions: IDIOM, INDEX, INFER, INPUT, INTRO, IRONY, IVORY
// J additions: JELLY, JOINT, JOKER, JOLLY, JOUST, JUMPY, JUROR
// K additions: KHAKI, KNACK, KNAVE, KNEEL, KNELT
// L additions: LABEL, LABOR, LAPSE, LARGE, LATCH, LATER, LAYER, LEAFY, LITHE, LOANS, LOCAL, LOFTY, LOGIC, LORRY, LUCID, LUSTY, LYING, LYRIC
// M additions: MANOR, MANGO, MANIC, MANLY, MARCH, MARSH, MEANT, MEDIA, MELON, MERIT, MESSY, METRO, MICRO, MIGHT, MIRTH, MIXER, MODEL, MOCHA, MONEY, MOLDY, MOSSY, MOTIF, MOUND, MUDDY, MURKY, MUTED
// N additions: NASTY, NAVAL, NICER, NOISY, NOTCH, NUDGE, NURSE
// O additions: OASIS, OCEAN, OFFER, ONSET, OPTIC, OUGHT, OUNCE, OVERT, OWNER, OXIDE, OZONE, OFTEN
// P additions: PANIC, PAPER, PASTA, PEACE, PEDAL, PERCH, PILOT, PINCH, PITCH, PIXEL, PLACE, PLAID, PLANK, PLAZA, PLUMB, PLUMP, PLUNK, POINT, POKER, POLKA, POUCH, PRANK, PRAWN, PRINT, PRIVY, PROXY, PSALM, PUNCH
// Q additions: QUICK, QUILL, QUITE, QUEUE, QUIRK
// R additions: RABBI, RANCH, RANGE, RAPID, RATIO, REPAY, RESIN, RIDER, RIDGE, RIGHT, RIGID, RIVET, ROAST, ROBOT, RODEO, ROUGE, ROUND, ROYAL, RUGBY, RULER, RURAL
// S additions: SAUCE, SAUNA, SCORE, SCOUT, SEDAN, SEIZE, SENSE, SERVE, SEVER, SHACK, SHAFT, SHAKY, SHALL, SHAWL, SHORT, SHOUT, SHOWN, SIGHT, SILKY, SINEW, SIXTY, SKILL, SLASH, SLEEK, SLEET, SLICK, SLING, SMALL, SMEAR, SNIFF, SNOOP, SNORE, SNORT, SOBER, SONIC, SPREE, SQUAD, SQUAT, STAGE, STAID, STALK, STANK, STARK, STASH, STAVE, STOIC, STOOP, STRAP, STRUT, STUCK, STUMP, STUNG, STUNK, SUPER, SWEPT, SWIPE, SWOOP, SYRUP
// T additions: TACIT, TAUNT, TEETH, TEPID, TERSE, TESTY, THEFT, THEME, THOSE, THREW, THREE, THUMP, TIPSY, TOKEN, TORSO, TOWEL, TOXIC, TRAIT, TRAMP, TRAWL, TREND, TRIPE, TROLL, TROOP, TUBER, TUMOR, TUNIC, TUTOR, TWIRL
// U additions: UNFIT, UNIFY
// V additions: VALID, VALOR, VALVE, VAPOR, VERGE, VOTER
// W additions: WAGER, WAGON, WAIST, WEIGH, WHACK, WHIFF, WHIRL, WHISK, WIRED, WITTY, WOMAN, WORLD, WORRY, WOVEN, WRING, WRIST
```

When inserting, merge each letter's additions into the existing letter group. For example, under `// B`, the existing words start with BADGE — place new B words in the correct alphabetical position within that group. Keep all words uppercase and quoted. Maintain the existing comma-separated-string-per-line formatting style.

Do NOT add BEARD (already in PUZZLE_WORDS at line 734), LANCE (already at line 769), BLOWN (check — it is at line 737 already), PURSE (already at line 781). Before inserting, scan the existing Set for any collisions and skip those words silently.

After insertion, the Set should have approximately 667 entries total.
  </action>
  <verify>
Open ladder.html in a browser (file:///C:/Users/ananW/jacktest/ladder.html) and open DevTools console. Run:
```javascript
console.log('PUZZLE_WORDS size:', PUZZLE_WORDS.size);
console.log('Has SCARE:', PUZZLE_WORDS.has('SCARE'));
console.log('Has STILL:', PUZZLE_WORDS.has('STILL'));
console.log('Has STARE:', PUZZLE_WORDS.has('STARE'));
console.log('Sample new words:', PUZZLE_WORDS.has('NURSE'), PUZZLE_WORDS.has('WORLD'), PUZZLE_WORDS.has('QUICK'));
```
Expected: size >= 600, all checks true.

Also verify no syntax errors: the page should load without console errors.
  </verify>
  <done>PUZZLE_WORDS.size is approximately 667 (600+ acceptable), no duplicate entries visible in the Set, page loads without JavaScript errors, spot-check of new words (NURSE, WORLD, QUICK, TACIT) all return true from PUZZLE_WORDS.has().</done>
</task>

<task type="auto">
  <name>Task 2: Fix getDailyLadderPuzzle() — step range, path cap, fallback pair, comment</name>
  <files>ladder.html</files>
  <action>
In ladder.html, locate `function getDailyLadderPuzzle()` (around line 901). Make exactly four edits inside this function:

**Edit 1 — Tighten step range (line ~927):**
Find:
```javascript
        if (!path || path.length < 4 || path.length > 8) continue;
```
Replace with:
```javascript
        if (!path || path.length < 5 || path.length > 7) continue;  // 4-6 steps
```
Note: path.length 5 = 4 steps (start + 3 intermediates + end = 5 words = 4 changes). path.length 7 = 6 steps.

**Edit 2 — Add common-path length cap (line ~931):**
Find:
```javascript
        const commonPath = bfsShortestPath(start, target, COMMON_ADJACENCY);
        if (!commonPath) continue;
```
Replace with:
```javascript
        const commonPath = bfsShortestPath(start, target, COMMON_ADJACENCY);
        if (!commonPath || commonPath.length > 11) continue;  // must exist, <=10 steps
```

**Edit 3 — Replace broken fallback pair (line ~936-937):**
Find:
```javascript
      // Absolute fallback: known-good common-word pair
      return { start: 'STONE', target: 'CRANE', optimalPath: bfsShortestPath('STONE', 'CRANE', ADJACENCY) };
```
Replace with:
```javascript
      // Absolute fallback: verified common-word pair, 4 steps
      // Path: SCARE->STARE->STALE->STALL->STILL (all common words)
      return {
        start: 'SCARE',
        target: 'STILL',
        optimalPath: bfsShortestPath('SCARE', 'STILL', ADJACENCY)
      };
```

**Edit 4 — Update the comment block (lines ~911-915):**
Find the comment above the for loop:
```
      // Try up to 50 seed offsets to find a valid pair where:
      //   1. Optimal BFS path (full dict) has 3-7 steps — sets difficulty
      //   2. A path exists through COMMON_ADJACENCY (common words only) —
      //      guarantees players can always solve without hitting obscure words.
      //      Optimal path may still use uncommon words for a shorter route.
```
Replace with:
```
      // Try up to 50 seed offsets to find a valid pair where:
      //   1. Optimal BFS path (full dict) has 4-6 steps — sets difficulty
      //   2. A path exists through COMMON_ADJACENCY (common words only) <=10 steps —
      //      guarantees players can solve without obscure words in a reasonable path.
```

Do not change any other part of getDailyLadderPuzzle(). The puzzlePool filter, the attempt limit (50), the DATE_SEED usage, and the return structure all remain unchanged.
  </action>
  <verify>
In the browser DevTools console after loading ladder.html:
```javascript
// Verify the new step range is active
const p1 = bfsShortestPath('SCARE', 'STILL', ADJACENCY);
console.log('SCARE->STILL path length:', p1.length, '(expect 5 = 4 steps)');

// Verify commonPath check
const p2 = bfsShortestPath('SCARE', 'STILL', COMMON_ADJACENCY);
console.log('Common path length:', p2.length, '(expect 5, well under 11)');
console.log('Common path:', p2);

// Verify fallback is reachable and returns correct pair
// Force it by temporarily checking: if STONE/CRANE no longer appear in source
```

Also grep the file to confirm no remaining references to STONE/CRANE as a fallback pair:
```bash
grep -n "STONE.*CRANE\|CRANE.*STONE" /c/Users/ananW/jacktest/ladder.html
```
Expected: zero matches (or only matches inside the PUZZLE_WORDS Set itself where CRANE still legitimately appears as a valid puzzle word).

Load the page in browser — confirm it loads with no console errors, and a puzzle is displayed (start word and target word shown in tiles).
  </verify>
  <done>
- `path.length < 5 || path.length > 7` is present in getDailyLadderPuzzle()
- `commonPath.length > 11` cap is present
- Fallback returns SCARE/STILL (not STONE/CRANE)
- Comment says "4-6 steps" (not "3-7 steps")
- Page loads without errors and displays a valid puzzle
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full system end-to-end:

1. Open ladder.html in browser, open DevTools console.
2. Run: `console.log('Pool size:', Object.keys(ADJACENCY).filter(w => w.length === 5 && PUZZLE_WORDS.has(w) && COMMON_ADJACENCY[w] && COMMON_ADJACENCY[w].length >= 1).length);`
   - Expected: meaningfully higher than the old value (~231), likely 280+.
3. Confirm the puzzle displayed has a reasonable difficulty indicator (optimal steps shown on results).
4. Grep for the broken fallback: `grep -n "STONE.*CRANE" ladder.html` — should only appear in PUZZLE_WORDS (CRANE is still a valid puzzle word), not in the fallback return statement.
5. Confirm PUZZLE_WORDS.size >= 600 in console.
</verification>

<success_criteria>
- PUZZLE_WORDS has ~667 entries (min 600)
- getDailyLadderPuzzle() uses path.length 5-7 (4-6 steps)
- getDailyLadderPuzzle() rejects commonPath.length > 11
- Fallback is SCARE->STILL with verified 4-step common-word path
- ladder.html loads without JavaScript errors
- A valid puzzle is displayed on page load
</success_criteria>

<output>
After completion, create `.planning/phases/16-ladder-polish/16-01-SUMMARY.md` following the summary template.
</output>
