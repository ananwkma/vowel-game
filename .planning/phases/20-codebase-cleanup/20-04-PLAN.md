---
phase: 20-codebase-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - cipher.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "cipher.html CSS sections follow the standard ordering with SECTION: format (GOOGLE FONTS → CSS VARIABLES → GLOBAL RESET → BACK TO HUB → LAYOUT → COMPONENTS → ANIMATIONS → RESPONSIVE)"
    - "cipher.html JS sections follow the standard ordering: CONSTANTS & CONFIG → STATE → UTILITY FUNCTIONS → DOM REFERENCES → ENGINE → UI RENDERING → EVENT HANDLERS → INIT"
    - "cipher.html no longer contains inline seededRandom or URL param parsing — these come from shared.js"
    - "Cipher game plays identically after restructure: block tapping, letter typing, cipher solving, give-up/reveal all work"
    - "No console errors on cipher.html load"
  artifacts:
    - path: "cipher.html"
      provides: "Cipher game — fully restructured with standard section ordering"
      contains: "shared.js"
  key_links:
    - from: "cipher.html"
      to: "shared.js"
      via: "<script src='/shared.js'> before game script block"
      pattern: "src=\"/shared.js\""
---

<objective>
Perform a full readability-first restructure of cipher.html (1145 lines), adding CSS section markers (currently missing from CSS) and enforcing standard JS section ordering. Replace inline utility duplicates with shared.js references.

Purpose: cipher.html is the most CSS-comment-sparse file — its style block has no SECTION: markers at all. This plan adds them plus restructures the JS to match project standards.
Output: cipher.html fully restructured with CSS and JS section markers, shared.js utilities, no behavior change.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-codebase-cleanup/20-CONTEXT.md
@.planning/phases/20-codebase-cleanup/20-RESEARCH.md
@.planning/phases/20-codebase-cleanup/20-02-SUMMARY.md
@cipher.html
@shared.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS section markers and audit dead code in cipher.html</name>
  <files>cipher.html</files>
  <action>
cipher.html's `<style>` block currently has NO `SECTION:` markers (confirmed in pre-planning code review — CSS is one large unsectioned block). This task adds them.

**CSS section marker insertion:**
Read through the cipher.html `<style>` block and insert `/* ============================================================ SECTION: [NAME] ============================================================ */` dividers at the appropriate boundaries. Expected sections based on content analysis:

```
SECTION: GOOGLE FONTS       — @import for Playfair Display (if present; cipher may not use it)
SECTION: CSS VARIABLES      — :root block with cipher-specific variable bridge
SECTION: GLOBAL RESET       — *, body, html base styles
SECTION: BACK TO HUB        — #back-link fixed positioning
SECTION: LAYOUT             — #app, main containers
SECTION: TITLE              — .game-title or header area
SECTION: CIPHER BLOCKS      — .cipher-block, .number-slot, .letter-slot — the core cipher grid
SECTION: KEYBOARD           — on-screen keyboard if present, or key input styles
SECTION: RESULTS SCREEN     — results overlay, sharing section
SECTION: HELP MODAL         — instructions/help overlay
SECTION: ANIMATIONS         — @keyframes, transition classes
SECTION: RESPONSIVE         — @media queries (collect all at end)
```

Adjust section names to match actual content found in cipher.html — don't force a section if the content isn't there.

**CSS variables bridge audit:**
cipher.html currently has a minimal `:root` block (confirmed: just `--cipher-block-size` and `--color-medium-charcoal`). Expand this bridge section to explicitly map any shared tokens that cipher.html uses directly. For each `var(--color-*)` or `var(--spacing-*)` usage that references shared token names, add the bridge alias:
```css
:root {
  --cipher-block-size: var(--block-size);
  --color-medium-charcoal: #D8D5CE;
  /* Bridge shared tokens to cipher-specific names */
  --color-cipher-bg: var(--color-bg);
  --color-cipher-primary: var(--color-primary);
  --color-cipher-text: var(--color-primary-text);
  --color-cipher-success: var(--color-success);
  --color-cipher-warning: var(--color-warning);
}
```
Only add bridge aliases for tokens actually used in the file. Do not add aliases for tokens not referenced.

**Dead code scan:**
1. Remove `console.log` calls not gated by IS_DEBUG.
2. Remove commented-out code blocks.
3. Remove TODO/FIXME comments referencing removed features.
4. Check for any undo/restart logic that was implemented but may have been superseded — verify it's still called before removing.

Do NOT rename CSS classes or change any layout/visual styles — only add section markers and dead code removal.
  </action>
  <verify>
Run: `grep "SECTION:" cipher.html` — must return at least 8 CSS section entries.
Run: `grep "console.log" cipher.html | grep -v "IS_DEBUG\|debug\|\[DEBUG\]\|\[Cipher\]\|\[DailyStatus\]"` — should return nothing.
Open cipher.html — confirm visual appearance unchanged (no layout regressions from CSS edits).
  </verify>
  <done>cipher.html CSS has SECTION: markers in standard order. Dead code removed. Bridge :root block covers all shared tokens cipher uses.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline utilities and enforce JS section ordering in cipher.html</name>
  <files>cipher.html</files>
  <action>
Complete the JS restructure of cipher.html following the shared.js pattern.

**1. Add shared.js script tag:**
Add `<script src="/shared.js"></script>` immediately before the main game `<script>` block (same placement as vowel.html and ladder.html — check 20-02-SUMMARY.md for exact placement decision).

**2. Remove inline duplicates (now in shared.js):**
cipher.html's JS currently has (confirmed via research — `SECTION: SEEDED PRNG` exists in cipher.html):
- Inline `seededRandom()` function — remove it.
- Inline URL param parsing / DATE_SEED computation — the research shows cipher.html has its own DATE_SEED IIFE. Replace with:
  `const CIPHER_DATE_SEED = DATE_SEED + '_cipher_v1';`
  Update all references to the old DATE_SEED to use CIPHER_DATE_SEED.
- `IS_DEBUG` declaration — remove (from shared.js now).
- `DailyStatus` object — remove if present (cipher may have its own DailyStatus block; check the `SECTION: PERSISTENCE` in cipher's JS — this is where it likely is). Remove it.

**3. Enforce standard JS section ordering:**
Reorder the JS script block to match the standard. Cipher's current JS sections (from research) are: SEEDED PRNG → QUOTE CORPUS → PERSISTENCE → GAME ENGINE → GIVE-UP & REVEAL → RESULTS & SHARING → HELP MODAL → UNDO/RESTART.

Remap to standard ordering:
```
SECTION: CONSTANTS & CONFIG — CIPHER_DATE_SEED, ANCHOR_PERCENT constant, etc.
SECTION: QUOTE CORPUS       — the quotes/puzzle data array (unchanged content)
SECTION: STATE              — gameState object
SECTION: UTILITY FUNCTIONS  — cipher generation helpers, local utilities
SECTION: DOM REFERENCES     — cached querySelector calls
SECTION: ENGINE             — buildCipherMap, selectDailyQuote, puzzle initialization
SECTION: UI RENDERING       — renderPuzzle, renderKeyboard, updateProgress, etc.
SECTION: GIVE-UP & REVEAL   — give up logic, pre-reveal logic
SECTION: RESULTS & SHARING  — showResults, buildShareText, etc.
SECTION: HELP MODAL         — help/instructions toggle
SECTION: EVENT HANDLERS     — all addEventListener calls
SECTION: INIT               — init() + DOMContentLoaded
```

Merge UNDO/RESTART into EVENT HANDLERS or ENGINE as appropriate.

**4. What NOT to change:**
- QUOTE_CORPUS array content (quotes and authors)
- Cipher decoding algorithm / cipherMap logic
- ANCHOR_PERCENT pre-reveal mechanic (Phase 19 feature — letters pre-revealed before game starts)
- localStorage keys and persistence logic
- Repetition filter logic (Phase 19 feature — prevents same quote on consecutive days)
- DailyStatus.markCompleted('cipher') call
- Keyboard interaction (tab navigation, hiddenInput typed letter routing)
- `_cipher_v1` seed namespace suffix
  </action>
  <verify>
Run: `node -e "const h=require('fs').readFileSync('cipher.html','utf8'); console.log('shared.js:', h.includes('shared.js')); console.log('no inline seededRandom:', !h.includes('function seededRandom')); console.log('no inline DailyStatus:', !h.includes('const DailyStatus')); console.log('CIPHER_DATE_SEED:', h.includes('CIPHER_DATE_SEED'));"` — all true.
Start server and open http://localhost:3000/cipher.html — tap a cipher block, type a letter, verify the letter appears in the block. Verify pre-revealed anchor letters are shown.
  </verify>
  <done>cipher.html references shared.js. No inline seededRandom, DailyStatus, or URL param IIFE. JS sections follow standard order with SECTION: format. Cipher game plays identically.</done>
</task>

</tasks>

<verification>
1. `grep "SECTION:" cipher.html` returns 8+ CSS entries and 8+ JS entries.
2. `grep "src=\"/shared.js\"" cipher.html` returns a match.
3. `grep "function seededRandom" cipher.html` returns nothing.
4. Open cipher.html — tap a number block, type a letter — no console errors.
</verification>

<success_criteria>
cipher.html fully restructured with CSS section markers added (was missing entirely), JS section ordering enforced, shared.js utilities referenced. Game plays identically.
</success_criteria>

<output>
After completion, create `.planning/phases/20-codebase-cleanup/20-04-SUMMARY.md`
</output>
